# Enable progress charts

Run **Drona: Set up LearningOS** to register the local **Flint-Chart MCP** into this workspace's
`.vscode/mcp.json`.

Then ask:

```
@drona /charts show my streak and mastery by topic
```

Charts render **locally** (SVG/PNG) — your data never leaves your machine.
