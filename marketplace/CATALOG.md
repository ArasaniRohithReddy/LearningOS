# LearningOS Marketplace — Catalog

> Auto-generated by `scripts/build-registry.mjs` on 2026-08-09. Do not edit by hand — re-run the generator.

**129 agents · 779 skills · 122 role configs**

## Packs (auto-grouped by domain)

### AI & ML  `(10)`

- **AI Engineer** — `@AI Engineer`
- **Azure AI Engineer** — `@Azure AI Engineer`
- **Computer Vision Engineer** — `@Computer Vision Engineer`
- **Deep Learning Engineer** — `@Deep Learning Engineer`
- **LLMOps Engineer** — `@LLMOps Engineer`
- **ML Engineer** — `@ML Engineer`
- **MLOps Engineer** — `@MLOps Engineer`
- **ML Platform Engineer** — `@ML Platform Engineer`
- **NLP Engineer** — `@NLP Engineer`
- **Prompt Engineer** — `@Prompt Engineer`

### Architecture  `(1)`

- **Solution Architect** — `@Solution Architect`

### Cloud & Platform  `(14)`

- **AWS Developer** — `@AWS Developer`
- **AWS DevOps Engineer** — `@AWS DevOps Engineer`
- **AWS Solutions Architect** — `@AWS Solutions Architect`
- **Azure Developer** — `@Azure Developer`
- **Azure Solutions Architect** — `@Azure Solutions Architect`
- **Cloud Engineer** — `@Cloud Engineer`
- **Cloud Native Engineer** — `@Cloud Native Engineer`
- **GCP Cloud Architect** — `@GCP Cloud Architect`
- **Kubernetes Engineer** — `@Kubernetes Engineer`
- **Network Engineer** — `@Network Engineer`
- **Platform Engineer** — `@Platform Engineer`
- **Power Platform Developer** — `@Power Platform Developer`
- **Serverless Engineer** — `@Serverless Engineer`
- **Terraform and IaC Engineer** — `@Terraform and IaC Engineer`

### Data & BI  `(18)`

- **Analytics Engineer** — `@Analytics Engineer`
- **Azure Data Engineer** — `@Azure Data Engineer`
- **Big Data Engineer** — `@Big Data Engineer`
- **Data Analyst** — `@Data Analyst`
- **Data Architect** — `@Data Architect`
- **Data Engineer** — `@Data Engineer`
- **Data Governance Specialist** — `@Data Governance Specialist`
- **Data Scientist** — `@Data Scientist`
- **Database Administrator** — `@Database Administrator`
- **Database Reliability Engineer** — `@Database Reliability Engineer`
- **Databricks Engineer** — `@Databricks Engineer`
- **Fabric Engineer** — `@Fabric Engineer`
- **GCP Data Engineer** — `@GCP Data Engineer`
- **Looker Developer** — `@Looker Developer`
- **Power BI Developer** — `@Power BI Developer`
- **Snowflake Engineer** — `@Snowflake Engineer`
- **Streaming Data Engineer** — `@Streaming Data Engineer`
- **Tableau Developer** — `@Tableau Developer`

### Design, Docs & Advocacy  `(6)`

- **Accessibility Engineer** — `@Accessibility Engineer`
- **Content Strategist** — `@Content Strategist`
- **Design Systems Engineer** — `@Design Systems Engineer`
- **Developer Advocate** — `@Developer Advocate`
- **Technical Writer** — `@Technical Writer`
- **UX and UI Designer** — `@UX and UI Designer`

### DevOps & SRE  `(9)`

- **Chaos Engineer** — `@Chaos Engineer`
- **DevOps Engineer** — `@DevOps Engineer`
- **FinOps Engineer** — `@FinOps Engineer`
- **Linux Administrator** — `@Linux Administrator`
- **Observability Engineer** — `@Observability Engineer`
- **Performance Engineer** — `@Performance Engineer`
- **Release Engineer** — `@Release Engineer`
- **Site Reliability Engineer** — `@Site Reliability Engineer`
- **Windows Server Administrator** — `@Windows Server Administrator`

### Emerging & specialized  `(8)`

- **AR and VR Engineer** — `@AR and VR Engineer`
- **Bioinformatics Engineer** — `@Bioinformatics Engineer`
- **Computer Graphics Engineer** — `@Computer Graphics Engineer`
- **Edge Computing Engineer** — `@Edge Computing Engineer`
- **GIS Developer** — `@GIS Developer`
- **HPC Engineer** — `@HPC Engineer`
- **Quantitative Developer** — `@Quantitative Developer`
- **Quantum Computing Engineer** — `@Quantum Computing Engineer`

### Enterprise Platforms  `(6)`

- **Blockchain Engineer** — `@Blockchain Engineer`
- **Dynamics 365 Developer** — `@Dynamics 365 Developer`
- **Robotics Engineer** — `@Robotics Engineer`
- **Salesforce Developer** — `@Salesforce Developer`
- **SAP ABAP Consultant** — `@SAP ABAP Consultant`
- **ServiceNow Developer** — `@ServiceNow Developer`

### Learning & Career Coaching  `(6)`

- **Career Mentor** — `@Career Mentor`
- **Coding Mentor** — `@Coding Mentor`
- **Exam and Certification Coach** — `@Exam and Certification Coach`
- **Interview Coach** — `@Interview Coach`
- **Meeting and Presentation Coach** — `@Meeting and Presentation Coach`
- **Research and News Analyst** — `@Research and News Analyst`

### Product & Delivery  `(5)`

- **Business Analyst** — `@Business Analyst`
- **Engineering Manager** — `@Engineering Manager`
- **Product Manager** — `@Product Manager`
- **Scrum Master and Agile Coach** — `@Scrum Master and Agile Coach`
- **Technical Program Manager** — `@Technical Program Manager`

### Programming languages  `(7)`

- **Clojure Developer** — `@Clojure Developer`
- **Dart Developer** — `@Dart Developer`
- **Elixir Developer** — `@Elixir Developer`
- **Haskell Developer** — `@Haskell Developer`
- **R Developer** — `@R Developer`
- **Scala Developer** — `@Scala Developer`
- **Swift Developer** — `@Swift Developer`

### QA & Testing  `(2)`

- **QA Automation Engineer** — `@QA Automation Engineer`
- **Software Development Engineer in Test** — `@Software Development Engineer in Test`

### Security  `(8)`

- **Application Security Engineer** — `@Application Security Engineer`
- **Cloud Security Engineer** — `@Cloud Security Engineer`
- **DevSecOps Engineer** — `@DevSecOps Engineer`
- **GRC Analyst** — `@GRC Analyst`
- **Identity and Access Management Engineer** — `@Identity and Access Management Engineer`
- **Privacy Engineer** — `@Privacy Engineer`
- **Security Engineer** — `@Security Engineer`
- **SOC Analyst** — `@SOC Analyst`

### Support & Growth  `(5)`

- **Customer Success Engineer** — `@Customer Success Engineer`
- **Growth Engineer** — `@Growth Engineer`
- **IT Support Engineer** — `@IT Support Engineer`
- **Localization Engineer** — `@Localization Engineer`
- **Sales Engineer** — `@Sales Engineer`

### Web & Software  `(23)`

- **Angular Developer** — `@Angular Developer`
- **API Designer** — `@API Designer`
- **Backend Engineer** — `@Backend Engineer`
- **C++ Developer** — `@C++ Developer`
- **C# and .NET Developer** — `@C# and .NET Developer`
- **Embedded and IoT Engineer** — `@Embedded and IoT Engineer`
- **Frontend Engineer** — `@Frontend Engineer`
- **Full-Stack Engineer** — `@Full-Stack Engineer`
- **Game Developer** — `@Game Developer`
- **Go Developer** — `@Go Developer`
- **Java Developer** — `@Java Developer`
- **Kotlin Developer** — `@Kotlin Developer`
- **Mobile Engineer** — `@Mobile Engineer`
- **Next.js Developer** — `@Next.js Developer`
- **Node.js Developer** — `@Node.js Developer`
- **PHP Developer** — `@PHP Developer`
- **Python Developer** — `@Python Developer`
- **React Developer** — `@React Developer`
- **Ruby on Rails Developer** — `@Ruby on Rails Developer`
- **Rust Developer** — `@Rust Developer`
- **TypeScript Developer** — `@TypeScript Developer`
- **Vue.js Developer** — `@Vue.js Developer`
- **WordPress Developer** — `@WordPress Developer`

## Skill packs (auto-grouped by domain)

### AI & data  `(32)`

- `/ab-test-designer` — Design a trustworthy A/B test / online controlled experiment: state a hypothesis, pick a primary metric and minimum detectable effect, compute sample size and power, randomize correctly, and avoid peeking and p-hacking.
- `/agent-designer` — Design an LLM agent: decompose the task, wire tool/function calling, choose memory, pick a planning loop (ReAct/reflection), integrate tools via MCP, add guardrails, and evaluate agentic behavior end to end.
- `/agent-evaluation-coach` — Evaluate an agent by its whole trajectory, not just its final answer — score tool-selection accuracy and argument validity, measure step efficiency against an optimal path, detect loops and thrashing, test recovery after a tool error, check goal completion and termination, build golden traces that replay deterministically in CI with mocked tools, and separate flaky-model failures from real regressions.
- `/agent-memory-coach` — Design what an AI agent remembers and what it deliberately forgets — separate the context window from real persistence, model episodic, semantic and procedural memory, define an explicit write-promote-forget policy with provenance and confidence, compact long conversations by summarizing instead of truncating, retrieve memories by relevance and recency rather than dumping everything, isolate sub-agent context so one worker cannot poison the whole run, and evaluate memory with recall, precision and staleness tests.
- `/ai-governance-coach` — Turn AI governance from a slide deck into an operating system — classify the system against the EU AI Act risk tiers, wire the four NIST AI RMF functions (Govern / Map / Measure / Manage) to named owners, publish a model card, and run a live risk register with real human-oversight controls.
- `/context-window-optimizer` — Optimize how an LLM app uses its finite context window: token budgeting, chunking, compression/summarization, memory (short/long-term), retrieval, and the 'lost in the middle' position effect.
- `/data-labeling-planner` — Plan a data-labeling / annotation project: write clear guidelines, sample what to label, measure inter-annotator agreement, run quality control, and pick tooling.
- `/dataset-explorer` — Guide exploratory data analysis (EDA) on a dataset as a lesson: understand columns and types, distributions, missingness, outliers, correlations, class balance, and target leakage — proposing the right visuals and next steps.
- `/embeddings-explainer` — Explain and choose embeddings plus vector search: what embeddings capture, similarity metrics (cosine/dot/L2), dimensionality, index types (flat/HNSW/IVF), and how to evaluate retrieval quality.
- `/eval-designer` — Design an evaluation for an ML or LLM system: define the task and metrics that reflect real success, build a representative and unbiased test set, choose automatic vs human vs LLM-as-judge scoring, and guard against data leakage and overfitting to the eval.
- `/feature-engineering-coach` — Teach feature engineering for a dataset: encode categoricals, scale numerics, build interactions and time/text features, and — above all — avoid data leakage, all tied to the chosen model and metric and validated properly.
- `/fine-tuning-data-curator` — Curate and format a fine-tuning dataset: select representative examples, format to the chat/instruction template, enforce quality and deduplication, make clean train/val/test splits, and avoid train-test leakage.
- `/fine-tuning-planner` — Decide and plan among prompting, RAG, and fine-tuning for a task — weighing cost/benefit, data needs, method (LoRA/QLoRA vs full), and evaluation, then picking the lightest approach that works.
- `/function-calling-coach` — Make LLM tool/function calling reliable — design tool JSON schemas the model can actually read (verb-first names, typed params, required vs optional, enums, descriptions that state when NOT to call), understand how and when the model decides to call, choose between single, parallel and sequential calls, validate and coerce arguments at the boundary, return tool results and errors the model can recover from, combine tool calls with structured outputs, and prevent the classic failure modes: hallucinated or invalid arguments, wrong tool chosen, and infinite tool loops.
- `/hallucination-mitigation-coach` — Reduce LLM hallucinations (confident, wrong output): ground with RAG, require citations, use self-consistency and self-checking, let the model abstain ('I don't know'), and evaluate faithfulness.
- `/hybrid-search-reranking-coach` — Fix retrieval quality by combining lexical and semantic search and then re-ranking — run BM25 alongside dense vector search, fuse the two ranked lists with Reciprocal Rank Fusion or score normalization, add a cross-encoder re-ranker over the top candidates, understand where ColBERT-style late interaction sits between bi-encoders and cross-encoders, choose candidate-K against a latency budget, and prove the gain with Recall@k, MRR and nDCG on a golden set rather than by looking at three queries.
- `/llm-cost-optimizer` — Cut LLM cost and latency without wrecking quality: model routing/tiering (small-first cascades), caching (prompt/semantic), batching, prompt/token reduction, output caps, and streaming — always behind a quality guardrail.
- `/llm-guardrails-designer` — Design defensive safety guardrails for an LLM app: validate inputs and outputs, defend against prompt injection and jailbreaks, handle PII, ground answers to reduce hallucination, and refuse out-of-scope or unsafe requests.
- `/mcp-server-builder` — Build a Model Context Protocol (MCP) server from first principles — decide what belongs in a tool vs.
- `/ml-experiment-tracker` — Set up experiment tracking and reproducibility for an ML project: log params, metrics, and artifacts, compare and reproduce runs, and version models in a registry (MLflow concepts).
- `/ml-pipeline-designer` — Design an ML training→serving pipeline: reproducible data splits, training, experiment tracking, a model registry, deployment (batch/online), and monitoring for drift — with reproducibility baked in.
- `/model-monitoring-coach` — Monitor a deployed model in production: detect data and concept drift, catch performance decay and data-quality breakage, set alerts and dashboards, and decide when to retrain.
- `/model-selection-advisor` — Choose the right ML model/approach for a problem: frame it (classification, regression, ranking, clustering, forecasting), start from a simple baseline, weigh model complexity against data size, and evaluate — with trade-offs made explicit.
- `/multi-agent-orchestration-coach` — Orchestrate multiple LLM agents that work together: roles/specialization, handoffs, shared state/memory, supervisor (orchestrator-worker) vs.
- `/preference-tuning-coach` — Choose and run the right alignment method — SFT to teach the format, RLHF/PPO to optimise a learned reward, or DPO to skip the reward model entirely — and understand what each one costs in data, compute, and failure modes.
- `/prompt-injection-defense` — Defend LLM applications against prompt injection — DEFENSIVE ONLY.
- `/prompt-optimizer` — Improve a prompt iteratively as a teaching exercise: clarify the goal and failure modes, apply proven techniques (clear instructions, few-shot examples, output structure, task decomposition), define a quick eval, and show a before/after diff.
- `/rag-designer` — Design a retrieval-augmented generation (RAG) pipeline as a lesson: chunking strategy, embedding model choice, vector store, retrieval and re-ranking, prompt assembly with citations, and evaluation — surfacing the trade-offs at each step.
- `/rag-evaluation-coach` — Measure whether a retrieval-augmented generation system is actually good — separate retrieval quality (recall@k, precision@k, MRR, nDCG, context precision and context recall) from generation quality (faithfulness/groundedness, answer relevance, completeness, citation correctness), build and maintain a golden question/answer set, choose between component-level and end-to-end evaluation, use LLM-as-judge with its biases controlled, and regression-test retrieval after every index, chunking, or embedding change.
- `/reasoning-models-coach` — Decide when to spend test-time compute — plain prompt, chain-of-thought, self-consistency, or a dedicated reasoning model — by computing the break-even against the cost of an error, and stop treating a visible chain of thought as a faithful explanation.
- `/structured-output-coach` — Get reliable structured output (JSON/objects) from an LLM: define a schema, use function/tool calling or JSON/structured-output modes, validate and repair, and use constrained/guided decoding for hard guarantees.
- `/vector-db-selector` — Choose and configure a vector database and index: pick a distance metric, compare index types (flat/exact, IVF, HNSW, PQ), trade recall against latency and memory, add metadata filtering, and plan for scale.

### Architecture & design  `(12)`

- `/api-design-review` — Review an API design (REST, GraphQL, or gRPC) as a lesson — resource modeling, HTTP semantics and status codes, naming, versioning, pagination, errors, auth, and idempotency — with concrete fixes and the principle behind each.
- `/architecture-diagram` — Produce a clear architecture diagram (Mermaid; C4-style context/container/component levels) for a system — identify components, data flows, and trust boundaries, then explain the design choices and trade-offs.
- `/consistency-models-coach` — Teach distributed-systems consistency for system design — the CAP theorem (and why 'pick 2' oversimplifies), PACELC, the consistency spectrum from linearizable to eventual, quorums (R + W > N) and tunable consistency, consensus (Raft/Paxos) at a conceptual level, and how to CHOOSE a model from a requirement.
- `/data-modeling-drill` — Practice data modeling end to end — turn requirements into an ER diagram (Mermaid) or a dimensional/star schema, choose keys and indexes, and normalize or denormalize with explicit trade-offs.
- `/domain-driven-design-coach` — Teach Domain-Driven Design end to end — strategic design (ubiquitous language, bounded contexts, subdomains, context maps and their integration patterns) and tactical design (entities, value objects, aggregates, invariants, domain events, repositories, domain services) — plus how to run an event storming session that derives those boundaries from real business behaviour instead of from a database schema.
- `/estimation-coach` — Teach back-of-the-envelope / capacity estimation — break the problem into knowns, use round numbers and powers of ten, compute QPS, storage, and bandwidth, then sanity-check the result.
- `/graph-data-modeling-coach` — Model connected data properly — property graph (nodes, typed directed relationships, properties on both) versus RDF triples and SPARQL, index-free adjacency versus repeated index joins, Cypher/GQL basics, and the decision of when a node beats a property beats a relationship — with the traversal-versus-join arithmetic that says whether a graph database is actually justified.
- `/hexagonal-architecture-coach` — Apply Alistair Cockburn's Ports and Adapters (hexagonal) architecture for real — identify driving vs driven ports, invert the dependencies so the domain owns its interfaces, push frameworks, ORMs and HTTP to the edge, and prove the core is testable with zero infrastructure and an automated dependency-direction check.
- `/openapi-spec-writer` — Write an OpenAPI (Swagger) specification as a lesson — model paths and operations, request/response schemas, parameters, status responses, reusable components, security schemes, and examples for a described API.
- `/tech-comparison` — Build a rigorous, weighted decision matrix comparing technologies or options — define criteria for the learner's context, score each option against cited evidence, and give a recommendation with caveats and a reversal trigger.
- `/threat-model` — Run an authorized, DEFENSIVE STRIDE threat-modeling exercise on a design — diagram trust boundaries and data flows, enumerate threats per element, rate risk, and propose mitigations to harden the system.
- `/transformer-architecture-explainer` — Explain a decoder-only transformer from first principles — tokens to embeddings, why Q/K/V exist at all, scaled dot-product and multi-head attention, causal masking, positional information (learned vs.

### Backend & systems  `(21)`

- `/api-pagination-coach` — Design list endpoints as a lesson — offset/limit vs cursor/keyset pagination, stable total ordering, filtering and sorting, page-size limits, and total-count trade-offs.
- `/api-versioning-coach` — Choose and defend ONE API versioning strategy — URI path vs custom header vs media-type negotiation vs additive-only evolution — classify every proposed change as breaking or non-breaking, then design the deprecation runway with the Sunset header (RFC 8594), a Link rel=sunset, and a dated removal.
- `/caching-strategy-coach` — Design a caching strategy as a lesson — decide what and where to cache (client, CDN, app/Redis, DB), pick a pattern (cache-aside, read-through, write-through, write-behind), set TTLs and eviction, and plan invalidation — with the consistency trade-offs behind each choice.
- `/circuit-breaker-coach` — Teach the circuit breaker resilience pattern from first principles — the closed/open/half-open state machine, failure-rate thresholds, reset timeouts and trial requests, fallbacks and graceful degradation, and how a breaker composes with timeouts, retries with exponential backoff and jitter, and bulkheads to stop cascading failure in a distributed system; plus where to place it, how to tune and observe it, and the anti-patterns (retry storms, breaker on the wrong boundary, breaker without a timeout).
- `/concurrency-coach` — Teach concurrency and parallelism as a lesson — threads vs async, CPU- vs I/O-bound, shared mutable state, locks and atomics, races and deadlocks, and safer patterns (immutability, channels, thread pools) — with the classic pitfalls.
- `/connection-pooling-coach` — Size a database connection pool with arithmetic instead of superstition — Little's Law, the PostgreSQL/HikariCP core-count formula, and the total-connections-across-all-instances budget — then diagnose pool exhaustion, pick a PgBouncer pool_mode (session/transaction/statement), and solve the serverless-per-invocation connection explosion.
- `/consensus-explainer` — Teach distributed consensus properly, starting from Raft (Ongaro & Ousterhout) — terms, leader election with the up-to-date restriction, log replication, quorums, and the commit rule — then contrast it honestly with Paxos, ZAB and Viewstamped Replication, and prove the safety argument on a concrete divergent-log scenario.
- `/cqrs-coach` — Teach Command Query Responsibility Segregation honestly — splitting the write model (commands, invariants, one consistent aggregate) from one or more read models/projections shaped for queries, when the asymmetry pays for itself (complex domain rules, wildly different read vs write shape or scale) versus when it is expensive ceremony over simple CRUD, its relationship with event sourcing (independent choices), the eventual consistency of read models and concrete ways to handle stale reads, plus the real costs and failure modes.
- `/database-index-coach` — Design database indexes as a lesson — read the query first, judge selectivity, choose the type (B-tree, hash, composite, partial, covering), order composite columns, verify with EXPLAIN, and weigh the write/space cost.
- `/event-sourcing-coach` — Teach event sourcing and CQRS as a lesson — model state as an append-only event log, build read-model projections, replay to rebuild, snapshot for performance, version events, and judge when it's overkill.
- `/graphql-schema-coach` — Design a GraphQL schema as a lesson — types and fields, queries/mutations/subscriptions, resolvers, the N+1 problem and DataLoader batching, cursor connections, nullability, and additive versioning.
- `/grpc-coach` — Design gRPC services with Protocol Buffers as a lesson — messages and field numbers, the four RPC/streaming types, wire compatibility and versioning, deadlines and status codes, and when gRPC beats REST.
- `/idempotency-coach` — Design idempotent APIs and operations as a lesson — idempotency keys, request deduplication, safe retries under at-least-once delivery, and making writes replay-safe.
- `/message-queue-coach` — Design async messaging as a lesson — choose queues vs pub/sub vs streams, pick a delivery guarantee (at-most/at-least/effectively-once), make consumers idempotent, decide ordering, add dead-letter queues, and handle backpressure.
- `/microservices-decomposer` — Decompose a monolith into services as a lesson — question whether to split at all, find boundaries with DDD (bounded contexts, business capabilities), assign data ownership, choose sync vs async, and count the operational cost.
- `/rate-limiter-designer` — Design rate limiting and throttling as a lesson — choose an algorithm (fixed/sliding window, token bucket, leaky bucket), pick the limit key (user/IP/API-key/global), enforce it in a distributed system, and define client behavior (429, Retry-After, backoff).
- `/retry-backoff-coach` — Design retry behaviour that heals transient faults without amplifying an outage — timeout budgets across a call chain, exponential backoff with full/decorrelated jitter, capped attempts, retry budgets and token buckets, deciding which errors are retryable, idempotency keys for safe replay, dead-letter queues and poison-pill handling, and avoiding retry storms and metastable failure.
- `/saga-pattern-coach` — Design distributed transactions with the saga pattern as a lesson — choreography vs orchestration, compensating actions, eventual consistency, isolation anomalies, and reliability with idempotent retries.
- `/schema-evolution-coach` — Change a data schema without breaking readers — classify the edit against the real resolution rules (Avro reader/writer matching, defaults, aliases and type promotion; Protobuf field numbers, reserved tags and unknown-field preservation), pick the right compatibility mode, and execute expand → migrate → contract with the correct producer/consumer deploy order.
- `/stream-windowing-coach` — Pick and defend a streaming window — tumbling, sliding/hopping, session, or global — then get the hard part right: event time vs processing time, watermark lag, allowed lateness/grace, triggers and early/late firings, accumulation mode, and where late records actually go.
- `/webhook-designer` — Design webhooks and event callbacks defensively as a lesson — event schema and versioning, delivery and ordering, retries with backoff, idempotency keys, HMAC signing and verification, and consumer ergonomics.

### Build & extend  `(1)`

- `/role-composer` — Compose a brand-new specialist agent from a small YAML role config — the config-driven way to add role-agents (Data Engineer, Azure AI Engineer, DevOps/SRE, Security, Power BI, etc.

### Career & communication  `(24)`

- `/case-study` — Build or analyze a real-world case study for learning — frame the problem and context, walk the approach and key decisions with their trade-offs, extract transferable lessons, and pose discussion questions.
- `/coding-interview-drill` — Run one timed coding-interview problem the technical/LeetCode way — present it, take clarifying questions, give progressive hints (never the answer), then review the learner's solution for correctness, complexity, and edge cases and reveal the optimal approach.
- `/cover-letter` — Coach a tailored, honest cover letter — extract the role's real needs, map the learner's actual experience to them, draft a tight three-paragraph letter in the learner's voice, and cut clichés and filler.
- `/customer-interview-coach` — Run non-leading discovery interviews using Rob Fitzpatrick's 'The Mom Test' — ask about the learner's past behaviour instead of their future opinions, refuse to pitch, dig for the last time the problem actually bit, and synthesise raw quotes into evidence-tiered insights instead of validation theatre.
- `/data-interview-drill` — Run one timed data analyst / data scientist interview task at a time — window-function SQL under the clock, A/B test interpretation, a 'metric dropped 15% — diagnose it' case, defining a metric from an ambiguous request, or a take-home dataset case study — then score it against an explicit rubric, show a model answer, and set one targeted follow-up.
- `/demo-script` — Script a live technical demo end to end — define the win, script a step-by-step happy path with what to say and show, add checkpoints and timing, and prepare fallbacks for when things break.
- `/em-interview-drill` — Run a scored engineering-manager interview loop — people scenarios (underperformance, conflict, attrition), hiring and team building, project and process judgement, and executive communication — one question at a time, with follow-up probes, a per-round rubric, and a model answer that shows the reasoning an EM interviewer is actually listening for.
- `/frontend-interview-drill` — Run one timed frontend interview task at a time — vanilla DOM/JS live-coding (debounce, throttle, event delegation, custom hooks), a practical UI build (autocomplete, modal, infinite list), component API design, rendering-performance debugging, an accessibility fix, or a frontend system design round — then score it against an explicit rubric, show a model answer, and set one targeted follow-up.
- `/interview-debrief-coach` — Run a blameless post-interview retro while the memory is fresh — reconstruct each question and the signal it probed, separate what you actually got wrong from what you merely felt bad about, classify the failure (knowledge, structure, communication, fit, or noise), and convert it into a dated, testable study plan.
- `/job-search-strategy-coach` — Run the job hunt as a funnel rather than a mood — build a tiered target list, set weekly input quotas you actually control, diagnose which stage is leaking, get past ATS and AI screens without keyword stuffing, and track every application in one honest pipeline.
- `/leadership-principles-drill` — Bar-raiser style interrogation of a story bank — take each STAR story and hammer it with the follow-ups a trained interviewer actually asks: 'what did YOU do', 'what was the data', 'what did you get wrong', 'what would you do differently' — then map stories to competencies and expose the gaps and the over-reused story.
- `/linkedin-optimizer` — Optimize a LinkedIn profile for a target role — mine recruiter keywords, rewrite the headline and About, sharpen experience bullets into quantified impact, list the right skills, and polish profile essentials.
- `/offer-evaluation-coach` — Model and compare job offers as year-by-year cash flows instead of headline numbers — build the vesting schedule (cliffs, front- vs.
- `/pm-interview-drill` — Run one timed product-management interview question at a time — product sense with CIRCLES, execution/metrics debugging ('DAU dropped 12%, diagnose it'), market-sizing estimation, and product strategy — then score the answer against an explicit rubric, show a model answer, and set one targeted follow-up.
- `/portfolio-reviewer` — Review a portfolio or GitHub profile as a lesson — assess the signal (pinned repos, project relevance, READMEs, commit history, contributions, profile bio) against a target role and give prioritized, concrete improvements.
- `/promotion-packet-builder` — Turn a year of work into a promotion case that a calibration room can defend — map real evidence to your ladder's criteria, prove sustained scope rather than one heroic quarter, quantify impact with baselines and measurement notes, recruit sponsors and cross-team witnesses, and name the gaps honestly with a plan to close them.
- `/resume-enhancer` — Strengthen a résumé/CV even without a target job description — turn duty-lists into quantified action-verb + impact (XYZ) bullets, fix scope/level signaling, tighten the summary, standardize structure and dates, add an ATS-friendly skills section, and remove filler.
- `/resume-tailor` — Tailor a resume to a specific role or job description — extract the JD's key requirements and keywords, map the learner's real experience to them, rewrite bullets as action-verb + quantified-impact (XYZ) statements, and flag honest gaps.
- `/salary-negotiation` — Prep the learner for a compensation negotiation — clarify the real offer and their leverage, anchor with cited market data they supply, script responses to common recruiter lines, and rehearse via role-play.
- `/slide-outline` — Turn a topic into a clear technical presentation outline — define the audience and goal, craft a narrative arc, and propose per-slide titles, key points, and suggested visuals, plus an opener and closer.
- `/sre-interview-drill` — Run one timed SRE / production-engineering interview round at a time — NALSD-style capacity-math system design, a Linux/production troubleshooting scenario, a live incident-command roleplay, or SLO and error-budget reasoning — then score it against an explicit rubric, show a model answer, and set one targeted follow-up.
- `/star-story-builder` — Build behavioral-interview stories with the STAR method — elicit a real experience, structure it into Situation, Task, Action, Result, sharpen the measurable result, and prep likely follow-ups.
- `/take-home-assignment-coach` — Scope, time-box, and ship a take-home assignment that earns the callback — decide what to build versus deliberately skip, hit the graders' actual rubric (correctness, tests, structure, README), resist gold-plating, and write the README that explains your trade-offs and stated non-goals.
- `/whiteboard-explainer` — Coach the learner to explain a concept out loud like an interview whiteboard/verbal round — prompt them to explain first, listen for gaps, jargon, and weak structure, ask the probing follow-ups an interviewer would, then give a crisp model explanation they can reuse.

### Code & engineering  `(42)`

- `/algorithm-visualizer` — Explain an algorithm or data structure by tracing it step by step on a concrete input — state the core idea and loop invariant, walk the execution with a step table and/or a Mermaid diagram, then give complexity and when to use it.
- `/bit-manipulation-coach` — Teach binary and bitwise thinking from first principles — two's complement, AND/OR/XOR/NOT and the shift operators, the reusable idioms (set/clear/toggle/test a bit, isolate the lowest set bit with x & -x, clear it with x & (x-1), popcount, power-of-two check, XOR to find the unique element, swap without a temp), subset enumeration with bitmasks, and the pitfalls (signed vs logical shifts, overflow, operator precedence, language differences).
- `/code-optimizer` — Optimize code for performance as a lesson — profile to find the real hotspot first, explain the dominant cost (algorithmic vs I/O vs allocation), apply the change, then quantify the trade-off in readability and memory.
- `/code-review-coach` — Review code as a teaching exercise, not just a critique.
- `/code-walkthrough` — Give a guided tour of an unfamiliar file, module, or repo — map the structure, trace the main execution flow, explain the key abstractions and how the pieces connect, and flag the best place to start reading.
- `/competitive-programming-drill` — Run one full competitive-programming rep the way the grind actually works — pick a pattern (two pointers, sliding window, binary search on answer, DP, graphs, greedy, backtracking, heap, union-find, trie, bitmask, number theory), get ONE original/classic problem with constraints, plan the complexity, code it, then execute it with #run against edge and stress cases and review from the REAL output.
- `/complexity-analyzer` — Analyze and teach time and space complexity — derive Big-O from the code, show the growth intuition with KaTeX math, identify the dominant term and the bottleneck, and compare alternatives.
- `/computability-complexity-coach` — Teaches theory of computation and complexity classes from first principles — Turing machines and automata, decidability and the halting problem, P vs NP vs NP-complete vs NP-hard, building reductions to prove a problem hard, and coping strategies (approximation, heuristics, parameterized/FPT, special cases).
- `/contest-prep-coach` — Prepare for competitive-programming contests end to end — explain the formats (Codeforces Div rounds, CodeChef Starters, AtCoder ABC/ARC/AGC, LeetCode weekly/biweekly), build a rating-ladder study plan, drill in-contest triage and problem order under time pressure, set up a template library with fast I/O, name the classic traps (overflow, off-by-one, TLE, edge constraints), and enforce disciplined upsolving and virtual contests.
- `/cpu-cache-performance-coach` — Teach mechanical sympathy from the memory hierarchy upward — cache lines and spatial/temporal locality, why row-major beats column-major, AoS vs SoA layout, false sharing and MESI ping-pong, branch prediction and mispredict cost, prefetching, and when SIMD actually pays — then hand over a compilable benchmark plus the exact perf counters that prove the diagnosis.
- `/debugging-coach` — Coach the learner through debugging as a repeatable method — reproduce, hypothesize, isolate, verify — so they can debug on their own next time instead of just being handed a fix.
- `/design-patterns-coach` — Apply the right design pattern to a real problem — recognize the need, name the GoF (or a few enterprise) pattern, implement it minimally, and warn against overuse.
- `/distributed-sql-coach` — Understand and evaluate distributed SQL / NewSQL engines — CockroachDB, Google Spanner, YugabyteDB — from the mechanism up: range sharding, one Raft group per range/tablet, leaseholders, TrueTime commit-wait versus hybrid logical clocks and uncertainty intervals, serializable versus strictly serializable, retry loops, hotspot primary keys, and the honest latency cost of surviving a region.
- `/dockerfile-coach` — Write or improve a Dockerfile as a lesson — choose the right base image, order layers for build-cache reuse, use multi-stage builds, shrink image size and attack surface, and add a .
- `/dsa-patterns-coach` — Pattern router and cheat-sheet for data structures and algorithms — given a problem or topic, identify which of the ~18 core pattern families applies (two pointers, sliding window, fast/slow pointers, merge intervals, cyclic sort, in-place linked-list reversal, BFS, DFS, backtracking, binary search on answer, top-K heap, K-way merge, subsets/combinations, topological sort, union-find, trie, bitmask, dynamic programming), explain the SIGNAL that triggers it, give the reusable template and typical time/space complexity, then route to the right deep-dive or drill.
- `/dynamic-programming-coach` — Teach dynamic programming from first principles — recognize optimal substructure + overlapping subproblems, DESIGN THE STATE, derive the recurrence and base cases, then go memoization → tabulation → space optimization across the classic families.
- `/error-handling-coach` — Teach robust error handling — exceptions vs Result/Either, error propagation, retries and idempotency, fail-fast vs graceful degradation, and the common anti-patterns.
- `/floating-point-numerics-coach` — Teaches IEEE 754 floating point from the bits up — sign/exponent/mantissa layout, why 0.
- `/functional-programming-coach` — Teach functional programming from first principles — pure functions, immutability, higher-order functions, composition, and (gently) monads — showing when FP helps and its trade-offs.
- `/git-coach` — Teach Git and resolve a Git situation safely — explain the model (commits, branches, refs, HEAD), give the exact commands with what each does, and flag destructive operations (history rewrite, force-push, reset --hard) with safer alternatives.
- `/graph-algorithms-coach` — Teach graph algorithms and — more importantly — WHICH one to reach for: representations, BFS/DFS, connected components, topological sort (Kahn + DFS), shortest paths (BFS, 0-1 BFS, Dijkstra, Bellman-Ford, Floyd-Warshall), MST (Kruskal + Prim), union-find/DSU, cycle detection, bipartite check, and SCC.
- `/greedy-algorithms-coach` — Teach when a greedy choice is provably optimal and when it quietly isn't — the two-part test (greedy-choice property + optimal substructure), the exchange argument as a proof technique, the classic greedy family (interval scheduling/activity selection, fractional knapsack, Huffman coding, minimizing maximum lateness, Dijkstra and Prim as greedy), how to construct a small counterexample when greedy is wrong, and the greedy-vs-DP decision.
- `/hash-table-internals-coach` — Deep-dive coach for hash tables and heaps: what makes a good hash function, separate chaining vs open addressing (linear/quadratic probing, Robin Hood, cuckoo), load factor and amortized O(1) resize, tombstones and deletion, hash-flooding DoS and randomized seeds, why iteration order is not a guarantee, and the sibling structure — binary heaps and priority queues.
- `/math-for-programming-coach` — Teach the mathematics that actually shows up in interviews and contests — modular arithmetic (including the negative-mod trap), fast modular exponentiation by squaring, gcd/lcm with the Euclidean algorithm, extended Euclid and modular inverses (Fermat vs extended-Euclid), primes via the sieve of Eratosthenes and trial-division primality, combinatorics (nCr, Pascal's rule, nCr mod p with factorials and inverse factorials), inclusion–exclusion, and overflow/precision discipline.
- `/memory-management-coach` — Teach how memory works — stack vs heap, allocation, and the models that free it (tracing/refcount garbage collection, manual malloc/free, and Rust ownership/borrowing) — plus common leaks and pitfalls.
- `/memory-model-lockfree-coach` — Teach concurrency at the level the hardware and the standard actually define — the happens-before relation, data races as undefined behaviour, std::memory_order relaxed/acquire/release/seq_cst and what each does NOT guarantee, compare-exchange loops and spurious failure, the ABA problem and its reclamation fixes, and the wait-free vs lock-free vs obstruction-free ladder — then hand over a compilable SPSC ring buffer checked with ThreadSanitizer.
- `/ml-system-design-drill` — Run one timed end-to-end ML system design round the way an applied-ML interview flows — frame the business problem into an ML problem, choose labels and data sources, design features and the feature store, pick a baseline and a model, design training and serving (batch vs.
- `/oop-design-coach` — Teach object-oriented design with SOLID — assign responsibilities, encapsulate state, favor composition over inheritance, and model a small domain cleanly.
- `/pair-programmer` — Pair-program a task interactively as driver and navigator — work in small steps, think aloud, ask the learner to predict and decide, write one slice, test it, then reflect — so they learn by doing, not by watching.
- `/probabilistic-data-structures-coach` — Teach sketches as an explicit accuracy-for-space trade — Bloom and cuckoo filters for membership, Count-Min for frequency, HyperLogLog for cardinality, skip lists for randomised ordered search, and consistent hashing for stable partitioning — always naming the error DIRECTION, the failure mode, and the formula that sizes the structure, then verifying it with a runnable script.
- `/recursion-backtracking-coach` — Teach how to think recursively and how to search systematically — base case vs recursive case, the leap of faith (trust the recursion), what the call stack actually holds, converting recursion to iteration and back, the backtracking template (choose, explore, un-choose) with pruning, the classics (subsets, combinations, permutations, N-queens, sudoku, word search, generating parentheses), recursion-tree complexity, and the pitfalls (missing base case, mutated shared state, stack overflow, redundant work that should be memoized).
- `/refactoring-coach` — Refactor existing code as a lesson, not a silent rewrite — spot the code smells, name the target improvement, then apply small safe steps behind a test safety net, explaining the principle (SOLID, DRY, cohesion/coupling) behind each change.
- `/regex-explainer` — Explain a regular expression part by part, or build one from a matching goal — break the pattern into a table of tokens, show what it matches (and doesn't) on real examples, warn about catastrophic backtracking, and give a tested final pattern.
- `/remote-code-runner` — Run and test code WITHOUT installing any language locally — execute snippets in 90+ languages via the free, keyless Piston public API (emkc.
- `/sorting-searching-coach` — Deep-dive coach for sorting and searching: why comparison sorts cannot beat n log n, stability and when it matters, how real libraries actually sort (Timsort, introsort, pdqsort, dual-pivot quicksort), quickselect for order statistics, linear-time counting/radix/bucket sorts and their preconditions, and binary search invariants that kill off-by-one bugs.
- `/sql-coach` — Improve SQL as a lesson — read the query, explain what it actually does, fix correctness (joins, NULLs, grouping), then optimize with indexes, sargable predicates, and execution-plan thinking, explaining every trade-off.
- `/string-algorithms-coach` — Teach efficient string processing from first principles — why naive matching degrades to O(nm), the KMP prefix function and the failure-link idea, Rabin–Karp rolling hashes with real collision care (double hashing, random bases, verify on match), the Z-algorithm, tries for prefix and multi-pattern search, palindromes via expand-around-center and the intuition behind Manacher, and when the problem actually needs a suffix array or automaton.
- `/system-design-drill` — Run a practice system-design problem end to end — clarify requirements and scale, do back-of-envelope estimates, propose an architecture with a Mermaid diagram, define the data model and APIs, then discuss bottlenecks and trade-offs, and score the answer against a rubric.
- `/test-writer` — Write tests as a teaching exercise — identify the behaviors and edge cases, then produce clear arrange-act-assert tests covering happy, edge, and error paths, explaining what each test protects and why (test pyramid, good naming).
- `/training-debug-coach` — Systematically diagnose a neural network that will not train — check the loss at initialisation against ln(C), overfit a single batch before touching hyperparameters, find NaN/Inf with anomaly detection and gradient norms, separate underfitting from overfitting from a data bug, run an LR range test instead of guessing, catch the classic silent errors (missing zero_grad, double softmax, forgotten model.
- `/tree-algorithms-coach` — Teach binary trees and BSTs from first principles — the four traversals (pre-order, in-order, post-order, level-order) with both recursive and iterative templates, the recursion-on-trees mental model (what do I ask my children, what do I return to my parent), height and diameter, lowest common ancestor, BST validate/insert/delete, tree DP such as subtree aggregates and rob-the-tree, plus the intuition for balanced trees, Fenwick/BIT and segment trees.
- `/type-system-explainer` — Explain a language's type system — static vs dynamic, generics, variance (co/contra), type inference, sum/product (algebraic) types, and nullability — with concrete examples.

### Communication & soft skills  `(7)`

- `/conflict-resolution-coach` — Work through a workplace conflict constructively — surface both sides' interests, separate the people from the problem, find common ground, and agree concrete next steps.
- `/email-writing-coach` — Write a clear, professional email — lead with the purpose, structure it for the reader, match tone to the audience, make the ask concrete, and craft a subject line, with a before/after rewrite.
- `/exec-communication-coach` — Rewrite technical updates into answer-first executive communication — BLUF opening, Minto Pyramid structure, MECE grouping, and the right container (one-pager, six-pager narrative, decision memo, status update, or escalation) — then score the draft against a rubric, show a rewritten model version, and set one targeted revision.
- `/feedback-giver` — Give constructive feedback that lands — use SBI (Situation-Behavior-Impact) to be specific and kind, turn it into an agreed action, and learn to receive feedback well too.
- `/negotiation-coach` — Prepare for a general negotiation — scope, deadlines, resources, or roles — using principled negotiation: separate interests from positions, know your BATNA, invent options for mutual gain, and decide on objective criteria.
- `/public-speaking-coach` — Prepare and deliver a talk with confidence — structure the content, craft the opening and closing, manage nerves, control pacing, and engage the audience, with a concrete rehearsal plan.
- `/time-management-coach` — Improve prioritization and focus — sort work with the Eisenhower matrix, protect deep work with timeboxing, cap work-in-progress, and tame interruptions, ending with a concrete weekly plan.

### Data & analytics  `(23)`

- `/airflow-dag-coach` — Design an Airflow orchestration DAG as a lesson — tasks and dependencies, idempotent operators, retries/backfills, sensors, and common anti-patterns — with explicit trade-offs.
- `/backfill-and-reprocessing-coach` — Re-run history without corrupting it — make every write idempotent, scope the work to explicit partition ranges, reset watermarks and stream offsets safely, choose between fix-forward, shadow-table swap and in-place replay, and cap the cost before the first byte is scanned.
- `/bigquery-optimization-coach` — Cut BigQuery cost and latency with mechanism, not folklore — partition and cluster on the real filter columns, prune before you scan, drop SELECT *, prove every change with bq query --dry_run bytes and INFORMATION_SCHEMA slot_ms, and choose between on-demand bytes-scanned and capacity slots deliberately.
- `/cdc-pipeline-coach` — Design a change-data-capture (CDC) pipeline as a lesson — log-based capture (Debezium), initial snapshots, event ordering, schema drift, and sinks/upserts — with explicit trade-offs.
- `/dashboard-designer` — Design an effective BI dashboard as a lesson — define the audience and the decisions, choose the right chart per question, structure layout and visual hierarchy, and avoid misleading visuals (truncated axes, chartjunk, pie overload).
- `/data-catalog-coach` — Stand up a data catalog and lineage as a lesson — technical + business metadata, ownership, searchable discovery, and column-level lineage for governance — with explicit trade-offs.
- `/data-contract-designer` — Define a data contract between producers and consumers as a lesson — schema, semantics, SLAs, versioning, and enforcement — with explicit trade-offs.
- `/data-observability-coach` — Instrument a data platform against the five pillars — freshness, volume, schema, distribution, lineage — so breakage is detected by a monitor rather than by a VP looking at a dashboard: set data SLOs, build seasonality-aware anomaly checks, route alerts by blast radius, and run a disciplined incident triage.
- `/data-pipeline-designer` — Design a batch ETL/ELT data pipeline as a lesson — sources, ingestion (full vs incremental/CDC), medallion transform layers (bronze/silver/gold), orchestration, idempotency, and data-quality gates, with explicit trade-offs.
- `/data-quality-checker` — Define data-quality checks for a dataset or pipeline as a lesson — the dimensions (completeness, validity, uniqueness, freshness, accuracy, consistency), executable tests, warn/error thresholds, placement, and alerting.
- `/data-vault-modeling-coach` — Teach Data Vault 2.
- `/data-warehouse-modeling` — Design a dimensional warehouse model as a lesson (Kimball) — pick the business process, declare grain, model facts vs.
- `/dbt-model-coach` — Build and structure dbt models as a lesson — staging/intermediate/marts layering, ref() and sources, generic + singular tests, docs, materializations (view/table/incremental/ephemeral), and incremental models — with trade-offs.
- `/dbt-semantic-layer-coach` — Define a metric ONCE and let every tool agree — teach the dbt Semantic Layer and MetricFlow properly: semantic models with entities, dimensions and measures, the reserved metric_time dimension, simple/ratio/derived/cumulative/conversion metric types, querying with dbt sl or mf, and governing definitions through version control instead of copy-pasted SQL.
- `/excel-formula-coach` — Teach Excel formulas and analysis as a lesson — lookups (XLOOKUP, INDEX/MATCH), logical and aggregation functions (IF/IFS, SUMIFS, COUNTIFS), dynamic arrays (FILTER, UNIQUE, SORT), and PivotTables — with the why and the failure modes.
- `/fabric-lakehouse-coach` — Teach Microsoft Fabric data engineering end to end with the trade-offs made explicit — OneLake as the single tenant-wide lake, shortcuts vs copying data, Lakehouse (Spark + read-only SQL analytics endpoint) vs Warehouse (full T-SQL, multi-table transactions), Direct Lake vs Import vs DirectQuery and what causes DirectQuery fallback, data pipelines vs Dataflows Gen2 vs notebooks, and capacity/SKU sizing.
- `/lakehouse-designer` — Design a lakehouse on an open table format as a lesson (Delta Lake / Apache Iceberg) — ACID on object storage, partitioning, compaction/clustering, and time travel — with explicit trade-offs.
- `/parquet-internals-coach` — Teach Apache Parquet from the bytes upward — file → row groups → column chunks → pages, dictionary/RLE and delta encodings, footer statistics, the page index and bloom filters — then show exactly how projection and predicate pushdown turn that layout into bytes NOT read, with a runnable metadata inspection.
- `/power-bi-dax-coach` — Teach and debug DAX for Power BI as a lesson — measures vs calculated columns, row vs filter context, CALCULATE and context transition, time intelligence, and the classic mistakes.
- `/snowflake-performance-coach` — Teach Snowflake performance and cost from its architecture — the separation of storage, compute (virtual warehouses) and cloud services — then make every knob explicit: warehouse size vs credits, auto-suspend and auto-resume, scale-up vs multi-cluster scale-out, micro-partitions and pruning, clustering keys and automatic clustering, the three caches, query profile reading, and dynamic tables with a target lag.
- `/spark-job-coach` — Write and optimize an Apache Spark job as a lesson — the lazy transformations-vs-actions model, narrow vs.
- `/streaming-pipeline-designer` — Design a real-time streaming pipeline as a lesson — event sources (Kafka), stream processing, windowing (tumbling/sliding/session), event-time vs.
- `/unity-catalog-coach` — Teach Databricks Unity Catalog governance from first principles — metastore and the three-level namespace catalog.

### Databases & storage  `(9)`

- `/database-migration-coach` — Plan a safe, low/zero-downtime schema or data migration as a lesson — use expand-contract, batched backfills, dual-writes, and feature flags, with a tested rollback for every step.
- `/database-selection-advisor` — Choose the right database as a lesson — match the workload (access patterns, consistency, scale) to relational vs document vs key-value vs wide-column vs graph vs time-series, weighing CAP, latency, and operational cost.
- `/mvcc-vacuum-explainer` — Explain multi-version concurrency control and the garbage it creates — row versions and snapshots, why readers never block writers, dead tuples and table/index bloat, HOT updates, the visibility map and index-only scans, autovacuum tuning (scale factors, cost delay, freeze thresholds), transaction-ID wraparound and aggressive freeze, plus how InnoDB differs with undo logs and the purge thread.
- `/nosql-data-modeling` — Model data for NoSQL as a lesson — design access-pattern-first for document, key-value, and wide-column stores, denormalize deliberately, and apply single-table (DynamoDB) patterns, weighing the trade-offs vs relational normalization.
- `/replication-topology-coach` — Choose and reason about database replication topologies — single-leader, multi-leader and leaderless (quorum) — covering synchronous vs asynchronous vs semi-synchronous commit, replication lag and read-your-writes, failover and split brain, fencing tokens, conflict resolution (LWW, CRDTs, application merge), quorum arithmetic (w + r > n) and how to route reads safely.
- `/sharding-strategy-coach` — Design database sharding/partitioning as a lesson — choose a shard key, compare hash vs range vs directory, plan rebalancing and hot-spot avoidance, handle cross-shard queries and transactions, and weigh the operational cost.
- `/sql-query-explainer` — Explain an unfamiliar or complex SQL query step by step — unpack CTEs, window functions, joins, and subqueries in logical execution order, state what each block returns and why, then summarize the final result set.
- `/storage-engine-explainer` — Explain how databases actually store bytes — B-tree (page-oriented, in-place update) vs LSM-tree (memtable, SSTables, compaction), write-ahead logging and checkpoints, the buffer pool and page replacement, read/write/space amplification and their trade-offs, plus row-oriented vs columnar layouts with compression and vectorized scans — so engine and schema choices become measurable rather than tribal.
- `/transaction-isolation-explainer` — Explain ACID and SQL isolation levels as a lesson — walk read-uncommitted → read-committed → repeatable-read → serializable and the anomalies each prevents (dirty, non-repeatable, phantom, lost update, write skew), with concrete interleaved examples.

### DevOps & Cloud  `(24)`

- `/alerting-strategy-coach` — Design an alerting strategy as a lesson — alert on symptoms not causes, decide page vs ticket vs dashboard, set thresholds with multi-window burn-rate, and kill noise and alert fatigue.
- `/aws-well-architected-review` — Review an architecture against the AWS Well-Architected Framework's six pillars — operational excellence, security, reliability, performance efficiency, cost optimization, and sustainability — surfacing risks as prioritized findings with fixes.
- `/azure-landing-zone-coach` — Design an Azure landing zone the Cloud Adoption Framework way — management group hierarchy, subscription topology, hub-spoke networking, Entra ID identity, and Azure Policy governance — so workloads land in a secure, scalable foundation.
- `/capacity-planning-coach` — Plan capacity and scaling as a lesson — model peak demand from growth, find per-instance limits with load testing, size headroom (Little's Law), autoscale on the right signal, and balance cost.
- `/ci-pipeline-builder` — Design a CI/CD pipeline as a lesson — stages (build, test, scan, deploy), dependency caching, artifacts, environments, approval gates, and rollback — with a sample config sketch for GitHub Actions, Azure DevOps, or GitLab CI.
- `/cloud-cost-optimizer` — Cut cloud spend as a lesson — find the biggest cost drivers, rightsize resources, buy commitments (reserved instances/savings plans/CUDs), tier storage, and kill waste — then produce a prioritized savings plan.
- `/cloud-migration-planner` — Plan a cloud migration with the 6 Rs — rehost, replatform, refactor, repurchase, retire, retain — by assessing the estate, mapping each app to a strategy, and sequencing waves by risk and dependency.
- `/cloud-private-connectivity-coach` — Teach private, non-internet access to managed services across all three major clouds — AWS PrivateLink and gateway endpoints, Azure Private Endpoint and Private Link, GCP Private Service Connect and Private Google Access — with the DNS resolution pitfalls that break every one of them.
- `/disaster-recovery-planner` — Plan disaster recovery and business continuity properly — derive RTO and RPO from business impact rather than guessing, design a backup strategy (3-2-1, immutability and air gap, retention, and the restore tests that make backups real), choose replication (synchronous vs asynchronous) and a failover/failback procedure, then pick a point on the DR ladder — backup and restore, pilot light, warm standby, multi-site active/active — using an explicit cost vs RTO vs RPO trade-off, and rehearse it with runbooks and game days.
- `/distributed-tracing-coach` — Instrument distributed tracing with OpenTelemetry as a lesson — model spans, propagate context across services, choose head vs tail sampling, and read a trace waterfall to find the latency culprit.
- `/dora-metrics-coach` — Teach and actually compute the DORA four keys — deployment frequency, lead time for changes, change failure rate, and failed deployment recovery time — from data you already have (git history, the GitHub Deployments API, incident records), interpret the throughput/stability pair honestly, and improve the underlying system instead of gaming the number.
- `/feature-flags-coach` — Use feature flags well — classify each flag by type (release, ops/kill-switch, experiment/A-B, permission/entitlement) because type determines lifetime, owner and testing strategy; decouple deploy from release; run progressive delivery with canaries, percentage rollouts and ring deployments; manage the full flag lifecycle and pay down flag debt; test a codebase that has flags without exploding combinatorially; and avoid the classic traps of stale flags, flags used as long-lived configuration, and nested conditionals nobody can reason about.
- `/gcp-project-structure-coach` — Structure a Google Cloud resource hierarchy — organization, folders, projects, IAM, VPC, and billing — for isolation, least-privilege security, and clean cost attribution.
- `/gcp-well-architected-review` — Review a Google Cloud design against the six pillars of the Google Cloud Well-Architected Framework — operational excellence, security/privacy/compliance, reliability, cost optimization, performance optimization, and sustainability — producing scored findings with severity, the exact gcloud check to run, and a prioritized remediation plan.
- `/git-workflow-designer` — Design a team Git branching and release workflow as a lesson — choose trunk-based, GitHub Flow, or GitFlow for the team's release cadence, then define branch naming, PR/review rules, CI gates, and merge/versioning conventions with explicit trade-offs.
- `/gitops-coach` — Teach GitOps for infrastructure and continuous delivery — the four principles (declarative, versioned in Git, pulled automatically, continuously reconciled), Git as the single source of truth, pull-based reconciliation with Argo CD and Flux, the app-of-apps pattern, drift detection and self-healing, environment promotion, secrets (Sealed Secrets / SOPS), and rollback by reverting a commit.
- `/kubernetes-manifest-coach` — Write and critique Kubernetes manifests as a lesson — Deployment, Service, ConfigMap, liveness/readiness/startup probes, resource requests and limits, securityContext, and common misconfigurations.
- `/logging-strategy-coach` — Design a structured logging strategy as a lesson — structured JSON events, log levels, correlation and trace IDs, what to log and what never to log (secrets/PII), sampling, and cost control.
- `/observability-plan` — Instrument a service for observability as a lesson — the three pillars (metrics, logs, traces), OpenTelemetry, which signals matter (RED/USE), controlling cardinality and cost, and turning signals into dashboards and alerts.
- `/oncall-runbook-coach` — Build on-call readiness as a lesson — an escalation policy, a runbook index, the first-open dashboards, an alert-to-action map, and a clean shift handoff.
- `/postmortem-facilitator` — Facilitate a blameless postmortem meeting as a lesson — prep and invites, walk the timeline together, run a group 5 Whys root-cause discussion, and leave with owned, tracked actions.
- `/serverless-designer` — Design a serverless architecture — functions, API gateway, event sources, managed data stores, cold starts, statelessness, and the pay-per-use cost model — plus a clear-eyed take on when NOT to go serverless.
- `/slo-designer` — Define SLIs, SLOs, and error budgets for a service as a lesson — choose good indicators (availability, latency, error rate, freshness), set targets from user expectations, compute the error budget, and tie it to on-call and release policy.
- `/terraform-module-coach` — Structure reusable Terraform as a lesson — modules, input variables and outputs, remote state and locking, provider and version pinning, and plan/apply discipline — with a small module sketch.

### Engineering management  `(6)`

- `/delegation-coach` — Delegate effectively without abdicating — decide task vs outcome, match how much autonomy to the person's skill and will (situational leadership), set clear success criteria, and follow up.
- `/engineering-culture-coach` — Build a healthy engineering culture on purpose — articulate values, turn them into everyday norms, align incentives, and adopt blameless practices — so quality and learning are the default.
- `/hiring-process-coach` — Design a fair, structured hiring process — a role scorecard, consistent interview loop, evidence-based rubrics, and deliberate bias reduction — so you hire on signal, not vibes.
- `/performance-review-coach` — Run a fair, evidence-based performance review — gather examples across the whole cycle, rate against a rubric, calibrate with peers, and name growth areas — while avoiding recency, halo, and central-tendency bias.
- `/team-health-coach` — Assess and improve team health — build psychological safety, run honest retros, read the signals (silence, churn, missed commitments, rising rework), and act — so the team learns and delivers.
- `/tech-debt-coach` — Manage technical debt like a portfolio — make it visible, classify it (Fowler's quadrant), quantify its impact in time/risk/dollars, and negotiate paydown against features.

### Enterprise platforms  `(4)`

- `/abap-rap-coach` — Coach for the ABAP RESTful Application Programming Model on a free SAP BTP ABAP environment trial — build the stack from CDS view entity to behavior definition and implementation class, expose it as an OData V4 service through a service definition and binding, add draft handling for Fiori Elements, and understand where managed, unmanaged and ABAP Cloud differ from classic ABAP.
- `/dataverse-modeling-coach` — Design Microsoft Dataverse data models that survive production — tables and ownership, the right column data types, 1:N/N:N relationships and cascade behaviour, local vs global choices, alternate keys for integration and upsert, and the security-role matrix of privileges by access level.
- `/power-fx-coach` — Coach for Power Fx as a declarative, Excel-like language on the free Power Apps Developer Plan — read and fix delegation warnings, keep Filter/LookUp/Sort work on the server, understand the non-delegable data row limit, use collections and named formulas deliberately, and handle errors with IfError/Errors instead of hoping.
- `/salesforce-governor-limits-coach` — Design Apex that never throws a LimitException — why multitenancy forces per-transaction governor limits, the sync vs async budgets, bulkification patterns (no SOQL/DML in loops, bind a Set, build a Map, one DML), query selectivity, heap and CPU-time control, and choosing between @future, Queueable, Batch, Scheduled and Platform Events.

### Frontend & web  `(13)`

- `/accessibility-remediation-coach` — Fix the accessibility issues an audit found — prioritize by user impact, apply the right semantic/ARIA/focus fix, and verify — teaching the pattern.
- `/animation-coach` — Add web animations and transitions that are smooth and accessible as a lesson — CSS vs JS, compositor-only properties, prefers-reduced-motion, and performance.
- `/component-designer` — Design a reusable UI component as a lesson — a clear props/API, state ownership, composition, accessibility, and testable boundaries, with the trade-off behind each call.
- `/css-layout-coach` — Teach modern CSS layout for a target design and build it step by step — Flexbox vs Grid, the box model, responsive units, and the common pitfalls that break layouts.
- `/design-tokens-coach` — Define and use design tokens as a lesson — naming, layering, scales (color/space/type), light/dark theming, and wiring them to CSS and components.
- `/form-design-coach` — Design robust web forms as a lesson — labels, the right input types, accessible inline validation, clear error messages, and forgiving submission UX.
- `/micro-frontend-coach` — Decide whether and how to build micro-frontends as a lesson — composition (build vs runtime), team boundaries, shared state/deps, and the real operational cost.
- `/mobile-state-management-coach` — Structure mobile app state as a lesson — unidirectional data flow, state hoisting, persistence, and surviving process death on iOS and Android.
- `/pwa-coach` — Build a progressive web app as a lesson — service workers, caching strategies, offline support, installability (manifest), and the pitfalls.
- `/responsive-design-coach` — Build responsive, mobile-first layouts as a lesson — breakpoints, fluid type/spacing, responsive images, and testing across viewports.
- `/seo-optimizer` — Improve technical SEO for a site as a lesson — semantic HTML, metadata, structured data, crawlability, sitemaps, and performance's role — measurable and honest about what SEO can't do.
- `/state-management-coach` — Choose and structure frontend state as a lesson — local vs global vs server state, when to reach for a store, and the anti-patterns to avoid.
- `/web-perf-audit` — Audit web performance against Core Web Vitals as a lesson — measure LCP/INP/CLS, find the real bottleneck (JS, images, fonts, network, third parties), and prioritize fixes by impact.

### Game development  `(6)`

- `/game-ai-coach` — Teach game AI from first principles — finite state machines (FSM), behavior trees (selectors, sequences, decorators), A* pathfinding on grids/navmesh with admissible heuristics, and steering behaviors (seek, flee, arrive, flocking).
- `/game-loop-coach` — Teach the game loop from first principles — the update-vs-render split, a fixed-timestep accumulator, delta-time (dt) motion, frame-rate independence, and determinism.
- `/game-networking-coach` — Teach real-time multiplayer networking from first principles — authoritative client-server vs peer-to-peer, client-side prediction and server reconciliation, entity interpolation, and lag compensation (server rewind).
- `/game-optimization-coach` — Teach game performance optimization from first principles — profile before guessing (CPU-bound vs GPU-bound), the frame budget (16.
- `/game-physics-coach` — Teach game physics from first principles — numerical integration (explicit vs semi-implicit Euler, Verlet), broad/narrow-phase collision detection (AABB, SAT), impulse-based collision response with restitution and friction, and numerical stability.
- `/shader-coach` — Teach shaders and the GPU from first principles — the programmable pipeline (vertex → rasterizer → fragment/pixel → output), what vertex vs fragment shaders compute, MVP-space transforms and UVs, and building one simple effect (scrolling UV or Fresnel rim light).

### General  `(32)`

- `/account-abstraction-coach` — Teach ERC-4337 account abstraction from the ground up — UserOperation, alt mempool, Bundler, the EntryPoint singleton, Paymaster sponsorship, smart accounts vs EOAs, the two-phase validation/execution gas flow, and DEFENSIVE security practices (replay protection, EntryPoint-only callers, scoped session keys, paymaster griefing limits).
- `/ai-red-team-drill` — Run a DEFENSIVE, authorized red-team drill against an LLM application you own: build a structured probe taxonomy (jailbreak, indirect prompt injection, sensitive-data disclosure, harmful content, excessive agency), score each probe with a repeatable rubric, compute an attack-success rate per category, and turn every failure into a named mitigation and a regression test.
- `/ann-index-tuning-coach` — Tune approximate-nearest-neighbour indexes with measurements instead of defaults — HNSW (M, efConstruction, efSearch) versus IVF-Flat and IVF-PQ (nlist, nprobe, m, nbits), the recall/latency/memory triangle, ground-truth construction, recall@k computation, and the filtered-search recall trap — in FAISS and pgvector.
- `/backpressure-coach` — Design backpressure into async and streaming systems — bounded queues, credit/demand-based flow control (Reactive Streams request(n), HTTP/2 WINDOW_UPDATE, TCP receive window), and the four-and-only-four responses to a slow consumer: slow the producer, buffer (bounded), shed load, or scale out — with the queueing arithmetic that shows why an unbounded queue fails as latency long before it fails as OOM.
- `/backup-recovery-drill` — Run a real backup-and-restore drill instead of trusting a green backup job — derive RPO and RTO from an actual failure timeline, pick full vs incremental vs differential with the right restore chain, configure PostgreSQL PITR with WAL archiving, verify restores with pg_verifybackup and a data checksum, and apply 3-2-1(-1-0) including immutable copies.
- `/cloud-dr-strategy-coach` — Choose and cost a cloud disaster recovery strategy — backup & restore vs pilot light vs warm standby vs multi-site active/active — by deriving RTO and RPO from a real incident timeline, mapping each rung onto concrete AWS, Azure and Google Cloud services, modelling the standing cost of each, planning cross-region data replication, and proving the plan with restore drills and game days.
- `/data-mesh-coach` — Decide honestly whether data mesh fits, then apply it correctly — Zhamak Dehghani's four principles (domain-oriented decentralised ownership, data as a product, self-serve data platform, federated computational governance), what a data product must guarantee to be usable, how federated governance becomes executable policy instead of a committee, and the organisational preconditions that make mesh an expensive mistake for small teams.
- `/diffusion-models-explainer` — Explain how diffusion models actually work, from the equations up: the forward noising process and its closed form, the reverse denoising process, why the network predicts noise rather than the image, the noise schedule and signal-to-noise ratio, DDPM versus DDIM sampling, the score-function connection, classifier-free guidance, and latent diffusion.
- `/distributed-locking-coach` — Teach distributed mutual exclusion honestly — why a lease can never be safe on its own, what the Kleppmann-vs-antirez Redlock debate actually disputes, how fencing tokens move safety from the lock service to the resource, and when etcd/ZooKeeper beats Redis — then classify your lock as 'for efficiency' or 'for correctness' and pick accordingly.
- `/distributed-training-coach` — Scale training across GPUs and nodes without cargo-culting — separate data parallelism from tensor/pipeline/model parallelism, run PyTorch DDP correctly (DistributedSampler, set_epoch, effective batch size, LR scaling), choose an FSDP/ZeRO sharding stage from an actual memory budget, use gradient accumulation and activation checkpointing when you are memory-bound, and compute communication volume and scaling efficiency so you can say *why* 8 GPUs gave you 5x instead of 8x.
- `/excalidraw-sketch-generator` — Generate hand-drawn-style Excalidraw diagrams as real `.
- `/feature-store-coach` — Decide whether you actually need a feature store, then design one correctly: offline versus online stores, point-in-time-correct joins that prevent label leakage, the training/serving skew that silently destroys production models, Feast concepts (entity, feature view, materialization), and the honest 'a table and a cache is enough' alternative.
- `/graph-rag-coach` — Design knowledge-graph-augmented retrieval (GraphRAG) as a lesson: extract entities and relations into a graph, detect communities, pre-summarise them for global questions, and route each query to graph, vector, or hybrid retrieval — with an honest account of when a graph is worth the extra indexing cost and when plain vector RAG already wins.
- `/imbalanced-data-coach` — Handle skewed class distributions without fooling yourself — retire accuracy in favour of PR-AUC and F-beta, decide honestly whether you need resampling at all, apply SMOTE/undersampling/class weights *inside* the cross-validation fold so nothing leaks, move the decision threshold instead of the data where that is enough, keep probabilities calibrated, and stratify every split so rare-class folds are not empty.
- `/lookml-coach` — Model a governed semantic layer in LookML — views, dimensions, dimension_groups and measures, explores and joins with the relationship parameter that makes symmetric aggregates work, SQL derived tables vs native derived tables vs persisted derived tables (PDTs) and incremental PDTs, access_grant / access_filter / sql_always_where for row-level governance, and the field-level fields parameter.
- `/model-card-writer` — Write a model card that a reviewer, an auditor, and the next engineer can all act on: the nine Mitchell et al.
- `/multi-tenancy-data-coach` — Choose and defend a tenant-isolation model for your data layer — silo (database per tenant), bridge (schema per tenant), or pool (shared tables + tenant_id) — then make it actually safe with PostgreSQL row-level security, fail-closed tenant context, noisy-neighbour controls, per-tenant keys and crypto-shredding, and a migration path between models.
- `/opc-ua-coach` — Teach OPC UA (IEC 62541) for industrial IoT from the address space up — nodes, references, NodeIds and namespaces, type definitions and companion specifications, the SecureChannel → Session → Subscription/MonitoredItem stack, security modes and policies, client-server vs Part 14 PubSub, and what interoperability and certification actually guarantee — with a free, fully local Python server and client.
- `/openxr-xr-basics-coach` — Teach portable XR development from the Khronos OpenXR object lifecycle upward — instance → system → session → reference spaces → action sets → the xrWaitFrame/xrBeginFrame/xrEndFrame loop — so one binary runs on Quest, SteamVR, WMR, and the open-source Monado runtime without vendor #ifdefs.
- `/org-design-coach` — Design engineering organisations for fast flow — apply the four Team Topologies team types (stream-aligned, platform, enabling, complicated-subsystem) and three interaction modes (collaboration, X-as-a-Service, facilitating), use Conway's law deliberately via the inverse Conway manoeuvre, cut teams along fracture planes, size them to Dunbar limits, measure and reduce team cognitive load, and publish a team API so dependencies stop being surprises.
- `/orm-performance-coach` — Find and fix ORM performance problems from the SQL up — detect select-N+1 by counting queries, choose between lazy, joined and select-in (batch) loading, understand why joined eager loading of collections multiplies rows and breaks LIMIT, stop over-fetching columns, and decide when to drop to raw SQL — with concrete recipes for SQLAlchemy, Django ORM, Hibernate/JPA, ActiveRecord and Prisma.
- `/platform-idp-coach` — Design an internal developer platform that engineers actually choose to use — treat the platform as a product with users and a roadmap, build paved-road golden paths instead of mandates, make self-service the default (templates, ephemeral environments, one-command provisioning), decide what Backstage or a portal is and is not for, and measure the platform with DevEx and DORA signals rather than adoption vanity metrics.
- `/prerequisite-graph-generator` — Turn a learning goal into a prerequisite DAG — decompose the topic into atomic concepts, state the dependency between each pair as a testable claim ('you cannot understand B until you can do A'), detect and break cycles, topologically sort into levels, and emit a labelled Mermaid graph plus a sequenced learning path with checkpoints.
- `/quantum-error-mitigation-coach` — Teach NISQ-era error mitigation honestly — zero-noise extrapolation, probabilistic error cancellation, measurement/readout mitigation, dynamical decoupling and twirling — what each assumes, its exponential sampling overhead, and why none of it is fault tolerance.
- `/recommender-systems-coach` — Design and evaluate recommenders that survive contact with production — choose between content-based, neighbourhood collaborative filtering and matrix factorisation, model implicit feedback with confidence weights instead of pretending clicks are ratings, rank with NDCG/MAP/Recall@k instead of RMSE, split by time so you never train on the future, always beat a popularity baseline before claiming progress, and handle user/item cold start explicitly.
- `/referral-outreach-coach` — Ask for a job referral honestly and effectively — map who can actually refer you (weak ties beat close friends), qualify the role before you ask, write a short specific message that makes it easy to say yes *or* no, run a bounded follow-up cadence, and handle silence, a decline, or a referral that doesn't convert with grace.
- `/release-versioning-coach` — Design a release process people can trust — apply Semantic Versioning 2.
- `/rls-and-data-masking-coach` — Design and verify row-level security and dynamic data masking without leaving a hole — PostgreSQL RLS policies and the exact difference between USING (filters existing rows, fails silently) and WITH CHECK (validates new rows, fails loudly), permissive vs restrictive policies, FORCE ROW LEVEL SECURITY and the owner/BYPASSRLS bypass, security_invoker views for column masking, plus Snowflake row access + masking policies and BigQuery row access policies and data policies.
- `/timeseries-db-coach` — Design and operate a time-series store — TimescaleDB hypertables and chunks, InfluxDB buckets and tasks, Prometheus TSDB blocks — with the maths that actually governs cost: series cardinality as a product of label cardinalities, retention and downsampling tiers (continuous aggregates, recording rules), delta-of-delta plus XOR compression, and chunk/block sizing.
- `/tokenizer-explainer` — Explain and demonstrate how text becomes tokens — the normalisation/pre-tokenisation/model/post-processing pipeline, BPE vs WordPiece vs Unigram vs byte-level BPE, why subword vocabularies exist at all, special tokens and their off-by-one traps, counting tokens with tiktoken and Hugging Face tokenizers, and why tokenisation silently drives API cost, context limits, latency and model behaviour (arithmetic, spelling, non-English text).
- `/xr-comfort-performance-coach` — Budget and defend XR comfort and performance from first principles — motion-to-photon latency, the per-frame millisecond budget at 72/90/120 Hz, judder vs reprojection (timewarp/spacewarp), fixed and eye-tracked foveated rendering, vection and sensory-conflict-driven locomotion design (teleport, snap turn, vignette, rest frames), and the comfort settings that must ship on by default — framed in portable OpenXR terms.
- `/zk-proofs-coach` — Teach zero-knowledge proofs from the three defining properties — completeness, soundness, zero-knowledge — up through arithmetic circuits and R1CS to the real engineering trade-offs of zk-SNARKs (tiny proofs, trusted setup) versus zk-STARKs (transparent, post-quantum, larger proofs), then actually build and verify a proof with circom and snarkjs.

### Hands-on labs  `(361)`

- `/act-github-actions-lab` — Hands-on lab to run GitHub Actions workflows locally with act (nektos/act) in Docker — no subscription, no cloud minutes.
- `/airflow-local-lab` — Hands-on lab: run Apache Airflow locally with Docker Compose — free, open-source, no subscription.
- `/alertmanager-lab` — Hands-on lab: run Prometheus Alertmanager locally with Docker — route, group, and deduplicate firing alerts to receivers with a routing tree.
- `/angular-basics-lab` — Hands-on Angular lab — standalone components, templates and the four binding types, services with dependency injection, and signals — by building one small app.
- `/angular-signals-lab` — Hands-on lab for Angular's signal-based reactivity — build a real app with signal(), computed(), effect(), signal inputs (input/model/output), linkedSignal, zoneless change detection, and RxJS interop via toSignal/toObservable, learning why a pull-based memoized graph beats Zone.
- `/ansible-automation-lab` — Hands-on Ansible lab you can run free on your own laptop — build an inventory, write an idempotent playbook out of `ansible.
- `/apex-soql-lab` — Hands-on Apex lab in a free Salesforce Developer Edition org — write Apex classes and triggers, query with SOQL and SOSL, apply the one-trigger-per-object handler pattern, bulkify DML and queries against governor limits, and prove it with @isTest unit tests and Test.
- `/argocd-local-lab` — Hands-on Argo CD lab on a local kind/k3d cluster — install the controller, register a Git repo, declare an Application CR, turn on automated sync with prune and selfHeal, deliberately drift the cluster and watch it repair, then order rollouts with sync waves and scale to many apps with app-of-apps and ApplicationSet.
- `/aws-apigateway-lab` — Hands-on AWS lab: build an API Gateway API end to end — choose REST vs HTTP API, define routes and methods, wire a Lambda proxy integration, add an authorizer, and deploy to a stage with throttling and logging.
- `/aws-cloudformation-lab` — Hands-on AWS lab for native infrastructure as code: write a CloudFormation template, preview the blast radius with a change set, deploy and update a stack, detect drift, and roll the same template across accounts and Regions with StackSets — then decide honestly between CloudFormation, CDK, and Terraform.
- `/aws-cloudfront-lab` — Hands-on AWS lab: put CloudFront in front of a private S3 bucket or an ALB — lock the origin with Origin Access Control (OAC), reason about cache keys, TTLs, and invalidations, restrict content with signed URLs and cookies, and choose between CloudFront Functions and Lambda@Edge.
- `/aws-cloudwatch-lab` — Hands-on AWS lab: build CloudWatch observability end to end — send and query logs, read built-in and custom metrics, create an alarm that notifies via SNS, and assemble a dashboard.
- `/aws-dynamodb-lab` — Hands-on AWS lab: model a DynamoDB table from access patterns — pick partition and sort keys, create the table, add a secondary index, and use Query instead of Scan.
- `/aws-ecs-lab` — Hands-on AWS lab: run a container on ECS end to end — write a task definition, choose Fargate vs EC2, create a service, and put it behind a load balancer.
- `/aws-eventbridge-lab` — Hands-on AWS lab: build event-driven fan-out with Amazon EventBridge — create a custom event bus, write rules with event patterns, attach multiple targets with an input transformer, add a DLQ and retry policy, schedule with cron/rate or EventBridge Scheduler, and archive then replay events.
- `/aws-iam-lab` — Hands-on AWS lab: work with IAM identities and permissions — create a role and least-privilege policy, set a trust relationship, and read a policy document's Effect/Action/Resource/Condition.
- `/aws-kms-envelope-encryption-lab` — Hands-on AWS lab: trace the envelope-encryption chain end to end — GenerateDataKey returns a plaintext DEK and an encrypted DEK, you encrypt locally, discard the plaintext, and store the wrapped key alongside the ciphertext.
- `/aws-lambda-lab` — Hands-on AWS lab: build a Lambda function end to end — handler code, a trigger (API Gateway/S3/EventBridge), an IAM execution role, environment config, CloudWatch logging, and cold-start tuning.
- `/aws-organizations-scp-lab` — Hands-on AWS lab for multi-account guardrails: design an OU tree, write service control policies, and reason precisely about SCP evaluation semantics — an SCP is a permissions ceiling, never a grant, and effective access is the intersection of SCP, RCP, and IAM.
- `/aws-rds-lab` — Hands-on AWS lab: stand up an Amazon RDS database (concepts) — pick an engine and instance, connect privately over a security group, tune settings with a parameter group, and understand automated backups, snapshots, and point-in-time recovery.
- `/aws-s3-lab` — Hands-on AWS lab: build an S3 bucket end to end — create a bucket, put/get objects, turn on versioning, add lifecycle rules, and lock down access with Block Public Access and a least-privilege policy.
- `/aws-sns-lab` — Hands-on AWS lab: build an SNS topic end to end — create a topic, add subscriptions, fan out one publish to many SQS queues and Lambdas, and apply subscription filter policies so each subscriber gets only what it needs.
- `/aws-sqs-lab` — Hands-on AWS lab: build an SQS queue end to end — create a Standard or FIFO queue, send and receive messages, delete after processing, tune the visibility timeout, and add a dead-letter queue with a redrive policy.
- `/aws-stepfunctions-lab` — Hands-on AWS lab: build a Step Functions state machine end to end — define states in Amazon States Language, invoke Lambda with Task states, branch with Choice, and add Retry and Catch for error handling.
- `/aws-vpc-lab` — Hands-on AWS lab: build a VPC network end to end — plan CIDR, create public and private subnets, attach an internet gateway and NAT, wire route tables, and contrast security groups with network ACLs.
- `/azure-aks-lab` — Hands-on Azure lab: stand up an Azure Kubernetes Service (AKS) cluster end to end — create the cluster and node pool, deploy a workload, expose it with a Service and managed NGINX ingress, then scale with the HPA and cluster autoscaler.
- `/azure-app-service-lab` — Hands-on Azure lab: deploy a web app on App Service and operate it like production — choose a plan tier from real constraints, add a staging slot and swap with zero cold start, bind a custom domain with a free managed certificate, and replace every secret in app settings with a Key Vault reference backed by a managed identity.
- `/azure-bicep-lab` — Hands-on Azure lab: author, deploy, and decompile Bicep end to end — write resource/param/var/output syntax, factor reusable modules, preview changes with what-if, target resource-group / subscription / management-group scopes, decompile existing ARM JSON to Bicep, and prove idempotency with a redeploy.
- `/azure-cosmosdb-lab` — Hands-on Azure lab: model an Azure Cosmos DB for NoSQL container from access patterns — choose a partition key, provision RU/s, pick a consistency level, and favor point reads over cross-partition queries.
- `/azure-entra-id-lab` — Hands-on Azure lab: work identity end to end in Microsoft Entra ID — create users and groups, assign built-in Azure RBAC roles at management-group / subscription / resource-group / resource scope, compare managed identity vs service principal, enable self-service password reset, and read effective access.
- `/azure-functions-lab` — Hands-on Azure lab: build an Azure Functions app end to end — pick a trigger and bindings, write an HTTP function, wire app settings and Key Vault-backed config, then monitor it in Application Insights.
- `/azure-keyvault-lab` — Hands-on Azure lab: secure secrets with Azure Key Vault end to end — create a vault, add secrets/keys, control access with Azure RBAC (over access policies), and read them from an app via managed identity and Key Vault references.
- `/azure-monitor-kql-lab` — Hands-on Azure lab: query Azure Monitor logs with KQL end to end — route diagnostic settings into a Log Analytics workspace, write where/summarize/bin/join/render queries, turn a query into a log search alert, compare it with a metric alert, wire an action group, pin results into a workbook, and control ingestion and retention cost.
- `/azure-servicebus-lab` — Hands-on Azure lab: build Azure Service Bus messaging end to end — create a namespace, choose queues vs topics/subscriptions, order with sessions, and handle poison messages via the dead-letter queue.
- `/azure-storage-lab` — Hands-on Azure lab: build Blob Storage end to end — create a storage account and containers, upload blobs, set access tiers and lifecycle rules, and lock down access with Microsoft Entra RBAC and a user-delegation SAS.
- `/azure-vnet-hub-spoke-lab` — Hands-on Azure lab: build the hub-spoke network everyone draws but few can trace — VNet peering (and why it is non-transitive), NSGs driven by application security groups, Azure Bastion for jump-box-free RDP/SSH, private endpoints with the privatelink DNS zones, and user-defined routes that force spoke traffic through a firewall in the hub.
- `/azurite-lab` — Hands-on Azure lab: practice Azure Storage locally and fully offline with Azurite, Microsoft's free open-source Storage emulator — no Azure subscription, no account, no real keys.
- `/bash-cron-lab` — Hands-on Bash lab on scheduling jobs with cron: the five crontab time fields, crontab -e/-l, capturing output to a log, absolute paths in cron's minimal environment, and idempotent jobs guarded by flock.
- `/bash-find-grep-lab` — Hands-on Bash lab on searching with find and grep: locating files by name, type, size, and time, searching contents recursively, bridging them with xargs -0, and deleting matches safely.
- `/bash-functions-lab` — Hands-on Bash lab on functions and arguments: defining functions, positional parameters ($1 $@ $#), shift, local variables, return codes vs printed output, and sourcing a reusable library.
- `/bash-pipes-lab` — Hands-on Bash lab on pipes and redirection: stdin/stdout/stderr (fd 0/1/2), building pipelines with |, redirecting with > >> 2> 2>&1, and process substitution <(.
- `/bash-scripting-lab` — Hands-on Bash lab on shell scripting: variables and quoting, conditionals (if/case), loops (for/while), functions, and strict mode set -euo pipefail.
- `/bash-text-processing-lab` — Hands-on Bash lab on text processing with grep, sed, and awk: filtering lines, substituting text, and extracting or summarizing fields from logs and CSVs.
- `/biopython-sequence-lab` — Hands-on, fully offline sequence bioinformatics with Biopython — parse FASTA and FASTQ with SeqIO, work with immutable Seq objects and SeqRecords, transcribe/translate with the right NCBI codon table, reverse-complement correctly, read Phred quality scores as probabilities, and score a pairwise alignment with Bio.
- `/browser-storage-lab` — Hands-on lab for client-side persistence — choose correctly between IndexedDB, Cache Storage, the Origin Private File System (OPFS), localStorage and cookies, then measure your quota with navigator.
- `/caddy-local-lab` — Hands-on lab: run Caddy locally to learn automatic HTTPS with its built-in local CA, reverse proxying, and the Caddyfile — free, open-source, no subscription.
- `/cadvisor-lab` — Hands-on lab: run cAdvisor locally with Docker — expose per-container CPU, memory, network, and filesystem metrics for Prometheus to scrape.
- `/cassandra-local-lab` — Hands-on lab: run Apache Cassandra locally with Docker — free, open-source, no subscription.
- `/chaos-engineering-lab` — Hands-on chaos engineering on a free local Kubernetes cluster — state a steady-state hypothesis in measurable terms, define an explicit blast radius and abort conditions, inject real faults (pod-kill, network latency and loss, CPU/memory stress, DNS failure) with Chaos Mesh, observe whether the hypothesis survives, and run the whole thing as a game day with a written record.
- `/chroma-vector-local-lab` — Hands-on Chroma lab — run the open-source Chroma vector database locally with no API key, no subscription, and no cost, fully offline.
- `/clickhouse-local-lab` — Hands-on ClickHouse for OLAP, entirely local and free via docker or the clickhouse-local binary — build a MergeTree table, understand why the primary index is sparse (one mark per index_granularity rows), choose ORDER BY versus PARTITION BY correctly, add data-skipping indexes, build incremental materialized views on AggregatingMergeTree, and prove granule pruning with EXPLAIN indexes=1.
- `/container-runtime-security-lab` — DEFENSIVE hands-on lab for hardening containers at runtime on your own machine — run as a non-root UID, mount the root filesystem read-only, drop every Linux capability, keep the default seccomp and AppArmor profiles on, block privilege escalation, enforce the `restricted` Pod Security Standard on a namespace, and watch Falco raise an alert when something unexpected happens inside a container.
- `/cosign-signing-lab` — Hands-on lab to run Cosign (Sigstore) locally as a defensive supply-chain tool — generate keys, then sign and verify your OWN container images and file artifacts (blobs) so consumers can prove integrity and provenance, with an optional keyless/OIDC flow.
- `/cpp-move-semantics-lab` — Teach C++ move semantics from the value-category rules upward — lvalue/xvalue/prvalue, what std::move actually is (a cast), move constructors and move assignment, the noexcept requirement, guaranteed copy elision vs NRVO, and perfect forwarding with reference collapsing — then hand the learner a compilable tracer program that prints exactly which special member ran.
- `/cpp-smart-pointers-raii-lab` — Hands-on C++ lab on RAII and ownership: replace raw new/delete with unique_ptr, shared_ptr and weak_ptr, use make_unique/make_shared, write custom deleters for C handles, reproduce and break a shared_ptr reference cycle, and apply the rule of zero/three/five.
- `/cpp-stl-templates-lab` — Teach the C++ standard library as a set of guarantees rather than a bag of classes — container complexity and the exact iterator-invalidation rules, the erase-remove idiom vs C++20 std::erase_if, <algorithm> and lazy std::ranges views, templates upgraded to concepts, and constexpr/consteval evaluation — then hand over a compilable program built with the libstdc++ debug and sanitizer flags that catch invalidation at runtime.
- `/cron-scheduler-lab` — Hands-on lab: schedule jobs locally with cron — free, built-in, no subscription.
- `/csharp-async-await-lab` — Hands-on C# lab on async/await: returning Task/Task<T>, awaiting without blocking, ConfigureAwait(false), cooperative cancellation with CancellationToken, and avoiding the sync-over-async deadlock.
- `/csharp-delegates-events-lab` — Hands-on C# lab on delegates and events: delegate types, built-in Func/Action/Predicate, lambda expressions, and the publisher/subscriber event pattern with EventHandler and safe ?.
- `/csharp-generics-lab` — Hands-on C# lab on generics: generic classes and methods, type-parameter constraints (where T : .
- `/csharp-linq-lab` — Hands-on C# lab on LINQ: query syntax vs method (fluent) syntax, deferred vs immediate execution, and core operators (Where, Select, OrderBy, GroupBy, First, Aggregate).
- `/csharp-nullable-lab` — Hands-on C# lab on nullable reference types: enabling the context, ? annotations, null-state flow analysis and its warnings, the null-forgiving operator (!), and null-check patterns.
- `/csharp-records-lab` — Hands-on C# lab on records and pattern matching: value-based equality, positional records, non-destructive with-expressions, and switch expressions with property, relational, and logical patterns.
- `/cuda-kernel-lab` — Write, launch, verify and profile a real CUDA kernel from first principles — thread/block/grid indexing and the global-index formula, the host/device memory hierarchy, memory coalescing, a worked vector-add followed by a shared-memory reduction, occupancy arithmetic, and Nsight Systems/Compute profiling — on your own GPU or a free Colab T4.
- `/cypress-test-lab` — Hands-on lab: write end-to-end tests locally with Cypress (free/OSS app) and drive them from its interactive test runner.
- `/dagster-local-lab` — Hands-on lab: run Dagster (open-source data orchestrator) locally — free, no subscription.
- `/data-cleaning-lab` — Hands-on lab on cleaning a messy DataFrame: quantify and handle missing values, fix wrong dtypes, drop duplicates, detect outliers, and normalize inconsistent strings — learning by running real code on dirty data.
- `/data-wrangling-lab` — Hands-on lab on reshaping and combining data: merge/join on keys, concat along an axis, pivot wide and melt long, and apply tidy-data principles — learning by running real code.
- `/dataweave-transform-lab` — Practise MuleSoft DataWeave 2.
- `/dbt-duckdb-lab` — Hands-on lab on dbt + DuckDB locally: build models, tests, and docs with zero cloud warehouse — free, local, and no subscription.
- `/debezium-cdc-lab` — Hands-on local lab on log-based change data capture with Debezium — stand up Postgres + Kafka + Kafka Connect with docker compose (free, no cloud account), register the Postgres connector, then read real change events: the before/after/source/op envelope, initial snapshot (op=r) versus streaming, REPLICA IDENTITY and what lands in before, tombstones on delete, incremental snapshots via the signal table, replication-slot/WAL growth and heartbeats, schema change events, and why delivery is at-least-once unless you opt into Kafka Connect exactly-once.
- `/delta-lake-lab` — Hands-on local lab on Delta Lake — build a table with delta-rs or Spark on your laptop (free, no cloud account), then open the _delta_log yourself: JSON commit files and their actions, checkpoints and _last_checkpoint, optimistic concurrency and conflict retry, MERGE for upserts and SCD Type 2, liquid clustering vs Hive-style partitioning, OPTIMIZE, VACUUM retention, and Change Data Feed.
- `/django-lab` — Hands-on Django lab: scaffold a project and app, define models with migrations, wire views and the URLconf, and get the admin CRUD UI for free.
- `/docker-compose-lab` — Hands-on lab on Docker Compose — build a multi-service app step by step: define services, a shared network, and named volumes, wire env/config, and order startup with depends_on + healthchecks.
- `/docker-networking-lab` — Hands-on lab on Docker networking — connect containers the free, local way with no subscription: compare the default bridge, a user-defined bridge with automatic DNS, and host mode; resolve services by name and publish ports.
- `/docker-registry-local-lab` — Hands-on lab to run a private Docker Registry locally with the open-source registry:2 image — no subscription, no Docker Hub — then tag, push, and pull your own images, list the catalog via the /v2 API, and persist data with a volume.
- `/docker-volumes-lab` — Hands-on lab on Docker volumes — persist container data the free, local way with no subscription: compare named volumes vs bind mounts, prove data survives container removal, and back up and restore a volume.
- `/document-parsing-lab` — Get real text out of real documents before it poisons your RAG index — pick the right parser for born-digital vs scanned PDFs, recover reading order and tables, run OCR only where it is needed, clean headers/footers/hyphenation, and chunk structurally instead of by blind character count.
- `/dotnet-webapi-lab` — Hands-on ASP.
- `/drone-ci-local-lab` — Hands-on lab to run a Drone CI pipeline locally from a .
- `/duckdb-lab` — Hands-on lab on DuckDB, an in-process (embedded) OLAP database: query Parquet and CSV and run analytical SQL on your laptop with no server — free, local, and no subscription.
- `/dynamodb-local-lab` — Hands-on AWS lab: practice Amazon DynamoDB locally and fully offline with DynamoDB Local, AWS's free official downloadable database — no AWS account, no subscription, no cloud keys.
- `/edge-ai-inference-lab` — Get a model running on-device within a real latency, memory, and power budget: post-training and quantization-aware quantization (per-tensor vs per-channel, and why that choice decides your accuracy), structured pruning and distillation, choosing between LiteRT (formerly TensorFlow Lite), ONNX Runtime, and Core ML, using NPU/GPU/DSP accelerators through execution providers and delegates, and measuring on the actual hardware.
- `/elasticsearch-local-lab` — Hands-on Elasticsearch lab — run Elasticsearch locally with Docker, free and offline, no cloud, no subscription.
- `/elixir-otp-genserver-lab` — Hands-on Elixir/OTP lab: spawn processes and message passing, links vs monitors, a full GenServer with init/handle_call/handle_cast/handle_info and state, Supervisors with one_for_one and rest_for_one restart strategies, and what 'let it crash' really means.
- `/email-auth-phishing-defense-lab` — A hands-on, authorized-domain-only lab for email authentication: publish and read SPF, sign and verify DKIM, then walk DMARC from p=none through quarantine to reject while proving identifier alignment — plus parsing aggregate (RUA) reports and layering BEC controls.
- `/envoy-local-lab` — Hands-on lab: run Envoy locally to learn its core objects — listeners, clusters, and routes — free, open-source, no subscription.
- `/esp-idf-lab` — Build real ESP32 firmware with Espressif's ESP-IDF — project layout and CMake, the idf.
- `/express-lab` — Hands-on Express.
- `/external-secrets-lab` — Hands-on External Secrets Operator on a free local cluster — install ESO, understand SecretStore vs ClusterSecretStore vs ExternalSecret, sync secrets from a backend (fake provider offline, then HashiCorp Vault in dev mode) into real Kubernetes Secrets, template the output, use dataFrom for whole-map sync, set refreshInterval for rotation, and diagnose SecretSynced conditions when it fails.
- `/fastapi-lab` — Hands-on FastAPI lab: declare path and query parameters, validate request bodies with Pydantic models, write async endpoints, and explore the auto-generated OpenAPI docs.
- `/firebase-emulator-lab` — Hands-on Firebase/GCP lab: practice Firebase locally and fully offline with the free official Firebase Local Emulator Suite — no billing account, no subscription, no real project keys.
- `/flask-lab` — Hands-on Flask lab: define routes with @app.
- `/flink-local-lab` — Run Apache Flink entirely on your own machine — no cluster, no cloud, no Kafka — and learn the DataStream API by building it: bounded sources, event time vs processing time, WatermarkStrategy, keyed windows, ValueState in a KeyedProcessFunction, side outputs for late data, and checkpoint/savepoint recovery.
- `/flink-sql-lab` — Hands-on local lab on stateful stream processing with Apache Flink SQL — run the SQL Client in Docker (free, no cloud), then build up dynamic tables and changelog streams, event-time watermarks, windowing TVFs (TUMBLE/HOP/CUMULATE), keyed aggregation state and state TTL, regular vs interval vs temporal vs lookup joins, and checkpoints for exactly-once recovery.
- `/floci-aws-local-lab` — Hands-on AWS lab: practice AWS locally and fully offline with the free, open-source Floci emulator — no cloud account, no subscription, no real credentials.
- `/floci-azure-local-lab` — Hands-on Azure lab: practice Azure locally and fully offline with the free, open-source Floci AZ emulator — no cloud account, no subscription, no auth token.
- `/floci-gcp-local-lab` — Hands-on GCP lab: practice Google Cloud locally and fully offline with the free, open-source floci-gcp emulator — no cloud account, no billing, no real credentials.
- `/floci-oracle-local-lab` — Hands-on Oracle Cloud (OCI) lab: practice OCI locally and fully offline with the free, open-source Floci OCI emulator — no cloud account, no subscription, no auth token.
- `/flutter-widgets-lab` — Hands-on Flutter lab on the widget model — the widget/element/render three-tree architecture, composition over inheritance, StatelessWidget vs StatefulWidget, the constraints protocol ('constraints go down, sizes go up, parent sets position'), ListView.
- `/foundry-forge-lab` — Hands-on Foundry lab running entirely on a free local toolchain — scaffold with forge init, write Solidity tests, drive them with cheatcodes like vm.
- `/gateway-api-lab` — Hands-on Kubernetes Gateway API on a free local cluster — install the standard-channel CRDs, understand the role-oriented split between GatewayClass, Gateway and HTTPRoute, migrate a legacy Ingress, then do header-based routing, path/method matching, weighted traffic splitting for a canary, request mirroring, and cross-namespace routes gated by ReferenceGrant.
- `/gcp-bigquery-lab` — Hands-on GCP lab: build a BigQuery analytics setup end to end — create a dataset and table, load data, run GoogleSQL, add partitioning and clustering, and control query cost.
- `/gcp-bigtable-emulator-lab` — Hands-on GCP lab: practice Cloud Bigtable locally and fully offline with the free official Bigtable emulator — no Google Cloud billing account, no subscription, no real credentials.
- `/gcp-cloud-functions-lab` — Hands-on GCP lab: build a Cloud Functions service end to end — an HTTP function, event triggers (Pub/Sub, Cloud Storage, Eventarc), environment/config variables, secrets, and Cloud Logging.
- `/gcp-cloud-run-lab` — Hands-on GCP lab: deploy a container to Cloud Run end to end — build and deploy a service, split traffic across revisions, tune concurrency and min/max instances, measure scale-to-zero cold starts, run a Cloud Run job, and lock the service down to invoker-only IAM.
- `/gcp-cloud-storage-lab` — Hands-on GCP lab: build a Cloud Storage setup end to end — create a bucket, pick a storage class (Standard/Nearline/Coldline/Archive), control access with IAM and signed URLs, and add lifecycle rules.
- `/gcp-datastore-emulator-lab` — Hands-on GCP lab: practice Cloud Datastore (Firestore in Datastore mode) locally and fully offline with the free official Datastore emulator — no Google Cloud billing account, no subscription, no real credentials.
- `/gcp-firestore-emulator-lab` — Hands-on GCP lab: practice Cloud Firestore locally and fully offline with the free official Firestore emulator (gcloud CLI or the Firebase Local Emulator Suite) — no Google Cloud billing account, no subscription, no real credentials.
- `/gcp-gke-lab` — Hands-on GCP lab: build a GKE workload end to end — create a cluster (Autopilot or Standard), deploy a containerized app, expose it with a Service, and add horizontal and cluster autoscaling.
- `/gcp-iam-lab` — Hands-on GCP lab: build an IAM setup end to end — grant roles (basic/predefined/custom) to principals, apply least privilege, read and set the allow policy, and audit access.
- `/gcp-pubsub-emulator-lab` — Hands-on GCP lab: practice Google Cloud Pub/Sub locally and fully offline with the free official Pub/Sub emulator — no Google Cloud billing account, no subscription, no real credentials.
- `/gcp-pubsub-lab` — Hands-on GCP lab: build a Pub/Sub messaging flow end to end — create a topic and subscription, choose push vs pull delivery, enable message ordering, and configure dead-lettering with retries.
- `/gcp-spanner-emulator-lab` — Hands-on GCP lab: practice Cloud Spanner locally and fully offline with the free official open-source Spanner emulator (Docker image gcr.
- `/gcp-storage-emulator-lab` — Hands-on GCP lab: practice Cloud Storage (GCS) locally and fully offline with the free open-source fake-gcs-server (fsouza) — no Google Cloud billing account, no subscription, no real credentials.
- `/gcp-tasks-emulator-lab` — Hands-on GCP lab: practice Cloud Tasks locally and fully offline with the free open-source gcloud-tasks-emulator (aertje/cloud-tasks-emulator) — no Google Cloud billing account, no subscription, no real credentials.
- `/gcp-vpc-networking-lab` — Hands-on Google Cloud lab: build a custom-mode VPC and trace traffic through it — regional subnets inside a global network, stateful firewall rules targeted by network tags or service accounts, Cloud NAT for egress without external IPs, Cloud DNS private zones, and the Shared VPC versus VPC Network Peering decision.
- `/geopandas-lab` — Hands-on, fully offline geospatial analysis with GeoPandas — GeoSeries/GeoDataFrame, the geometry column, CRS declaration and reprojection by EPSG code, metre-accurate buffers, spatial joins with predicates, overlay and dissolve, and static/interactive plotting — with the CRS-mismatch traps (degrees vs metres, Web Mercator area distortion, lon/lat axis order) taught explicitly.
- `/git-bisect-lab` — Hands-on Git lab on bisect: binary-searching commit history to find the exact commit that introduced a regression, marking good/bad revisions, skipping untestable commits, and automating the hunt with git bisect run.
- `/git-cherry-pick-lab` — Hands-on Git lab on cherry-pick: applying specific commits from one branch onto another, resolving cherry-pick conflicts, recording provenance with -x, picking ranges, and handling merge commits with -m.
- `/git-hooks-lab` — Hands-on Git lab on hooks: writing client-side pre-commit and pre-push scripts to run linters and tests, understanding the commit-msg hook, bypassing safely, and sharing hooks across a team with core.
- `/git-rebase-lab` — Hands-on Git lab on rebase: interactive rebase (squash, fixup, reword, reorder, drop), replaying commits onto a new base, autosquash, and the rebase vs merge trade-off — practiced safely on a throwaway branch.
- `/git-reflog-lab` — Hands-on Git lab on reflog and recovery: reading the reflog of HEAD moves, recovering commits lost to a bad reset --hard or rebase, rescuing work from a detached HEAD, and understanding gc expiry.
- `/git-submodules-lab` — Hands-on Git lab on submodules: adding a nested repo, cloning with --recurse-submodules, init/update, pinning to a specific commit, updating to a new upstream, and the common detached-HEAD and forgotten-push pitfalls.
- `/gitea-local-lab` — Hands-on lab to self-host Git locally with Gitea (lightweight, open-source, no subscription) in Docker — create repos, branches, pull requests, and run built-in Gitea Actions with a local runner.
- `/github-actions-oidc-lab` — Hands-on lab that deletes your long-lived cloud keys — configure GitHub Actions OpenID Connect federation to AWS, Azure, and Google Cloud, read the actual JWT claims (`iss`, `sub`, `aud`, `repository`, `environment`), write a trust policy that is scoped to one repo and one branch instead of a wildcard, and exchange the token for credentials that expire when the job ends.
- `/go-channels-lab` — Hands-on Go lab on channels: unbuffered vs buffered, send and receive, the select statement, directional channel types, and closing to build a pipeline.
- `/go-context-lab` — Teach Go's context.
- `/go-error-handling-lab` — Hands-on Go lab on errors: the error interface, returning error values, wrapping with %w, errors.
- `/go-generics-lab` — Hands-on Go lab on generics: type parameters, constraints (any, comparable, cmp.
- `/go-goroutines-lab` — Hands-on Go lab on goroutines: launching with the go keyword, the runtime scheduler and GOMAXPROCS, synchronizing with sync.
- `/go-interfaces-lab` — Hands-on Go lab on interfaces: implicit (structural) satisfaction, small single-method interfaces, type assertions with comma-ok, and type switches over any.
- `/go-modules-lab` — Hands-on Go lab on modules: go mod init, go.
- `/gradient-boosting-lab` — Learn gradient-boosted trees by building one — derive the regularised objective and the split gain, then tune XGBoost and LightGBM the way it actually works: low learning rate plus early stopping on a real validation set, depth-wise vs leaf-wise growth, and honest feature importance.
- `/gradio-local-lab` — Hands-on lab building a Gradio demo UI locally: wrap a Python function with gr.
- `/graphviz-dot-lab` — Hands-on Graphviz DOT lab — write valid DOT (graphs, nodes, edges, attribute defaults), group with subgraphs and `cluster_` boxes, control shape with rankdir/rank/ranksep/newrank, pick the right layout engine (dot vs neato vs fdp vs sfdp vs circo vs twopi), render to SVG/PNG/PDF, and gate parse errors in CI — plus an honest answer to when DOT beats Mermaid and when it doesn't.
- `/great-expectations-lab` — Hands-on lab on Great Expectations (GX Core) locally: define data-quality expectations and validate a dataset with a checkpoint and Data Docs report — free, local, OSS, and no subscription.
- `/groupby-aggregation-lab` — Hands-on lab on pandas groupby: split-apply-combine, aggregate with agg, reshape with transform, drop groups with filter, and group by multiple keys — learning by running real code.
- `/haproxy-local-lab` — Hands-on lab: run HAProxy locally to learn L4 (TCP) and L7 (HTTP) load balancing, backends, and health checks — free, open-source, no subscription.
- `/haskell-typeclasses-monads-lab` — Teach Haskell the way the type checker sees it — purity and referential transparency, lazy evaluation and WHNF, algebraic data types and pattern matching, typeclasses as constrained interfaces, and the Functor/Applicative/Monad ladder derived from the SHAPE of its operator plus its laws — then hand over a compilable module with a hand-written Monad instance and do-notation desugaring.
- `/helm-chart-lab` — Hands-on lab on Helm — package a Kubernetes app step by step: chart structure, values, templates, then install, upgrade, and rollback a release.
- `/hyperparameter-tuning-lab` — Hands-on, local, free hyperparameter optimisation — build a real search with scikit-learn and Optuna, understand why random search beats grid search when only a few hyperparameters matter, define search spaces on the right scale (log vs linear), prune bad trials early with median/Hyperband stopping, and wrap the whole search in nested cross-validation so the reported score is not the optimistic bias of the selection procedure itself.
- `/iceberg-table-lab` — Hands-on local lab on Apache Iceberg — create a table with pyiceberg or Spark on your laptop (free, no cloud account), then open the metadata tree yourself: metadata.
- `/ingestion-connector-lab` — Build a source connector that is safe to re-run — full refresh vs incremental sync, cursor/watermark selection and why '>' loses rows while '>=' plus an idempotent upsert does not, at-least-once delivery with state committed in the same transaction as the data, HTTP 429 handling with Retry-After and exponential backoff with full jitter, pagination, hard deletes that cursors cannot see, and schema drift.
- `/inp-optimization-lab` — Hands-on lab for Interaction to Next Paint — instrument real interactions with the Event Timing and Long Animation Frames APIs, split every slow one into input delay, processing duration and presentation delay, then fix the right third by yielding with scheduler.
- `/interpreter-build-lab` — Hands-on lab that builds a tiny programming language end to end — tokenizer, recursive-descent parser with precedence, AST, tree-walking evaluator, environments and closures — then shows what a bytecode VM changes and why.
- `/jaeger-tracing-local-lab` — Hands-on lab: run Jaeger all-in-one locally with Docker — send traces over OpenTelemetry (OTLP) and explore spans in the Jaeger UI.
- `/java-collections-lab` — Hands-on Java lab on the Collections Framework: choosing between List/Set/Map implementations (ArrayList vs.
- `/java-concurrency-lab` — Hands-on Java lab on concurrency: start threads with Runnable, submit work to an ExecutorService thread pool, protect shared state with synchronization and happens-before visibility, and compose async work with CompletableFuture.
- `/java-generics-lab` — Hands-on Java lab on generics: write generic classes and methods with type parameters, apply bounded wildcards using PECS (Producer Extends, Consumer Super), and understand type erasure and its runtime limits.
- `/java-optional-lab` — Hands-on Java lab on Optional: create values with of/ofNullable/empty, transform them with map/flatMap/filter, supply fallbacks with orElse/orElseGet/orElseThrow, and avoid anti-patterns like Optional.
- `/java-records-lab` — Hands-on Java lab on records and sealed types: declare compact immutable data carriers, validate invariants in a compact canonical constructor, model a closed hierarchy with sealed interfaces, and destructure with record patterns in an exhaustive switch.
- `/java-streams-lab` — Hands-on Java lab on the Streams API: build a map/filter/reduce/collect pipeline, understand lazy intermediate vs.
- `/java-virtual-threads-lab` — Hands-on Java lab on Project Loom: create virtual threads with Thread.
- `/jetpack-compose-lab` — Hands-on Jetpack Compose lab that builds one screen while proving each rule of the runtime — composition vs recomposition, state hoisting and unidirectional data flow, modifier order, LazyColumn with stable keys, side-effect APIs (LaunchedEffect/DisposableEffect/rememberUpdatedState), and the stability rule that Compose only skips composables whose parameters are stable.
- `/js-closures-lab` — A hands-on lab in JavaScript on closures and scope — lexical scoping, capturing variables by reference, the classic var-in-a-loop bug, and practical uses like data privacy and memoization.
- `/js-event-loop-lab` — A hands-on lab in JavaScript on the event loop — the call stack, macrotask vs microtask queues, timers (setTimeout), promise callbacks/queueMicrotask, and reasoning about execution order.
- `/js-generators-lab` — A hands-on lab in JavaScript on generators and iterators — function*, yield, the iterator and iterable protocols, and building lazy or infinite sequences consumed on demand.
- `/js-modules-lab` — A hands-on lab in JavaScript on modules — ES Modules vs CommonJS, named/default imports and exports, dynamic import() for code splitting, plus bundling and tree-shaking basics.
- `/js-promises-lab` — A hands-on lab in JavaScript on promises and async/await — chaining, error handling with try/catch, running work concurrently with Promise.
- `/js-prototypes-lab` — A hands-on lab in JavaScript on prototypes and this — the prototype chain, delegation and inheritance, how class desugars to prototypes, and the four this binding rules (default/implicit/explicit/new) plus arrow-function lexical this.
- `/jupyter-notebook-lab` — Hands-on lab on running Jupyter locally: notebooks, kernels, and reproducible analysis in JupyterLab with no subscription.
- `/k3d-lab` — Hands-on lab on k3d — run Rancher's lightweight k3s Kubernetes in Docker, free and local with no subscription: create a fast cluster, add a built-in local registry, deploy through the load balancer, and verify.
- `/k6-load-test-lab` — Hands-on lab: script and run a load test locally with Grafana k6, the free OSS tool.
- `/k8s-admission-policy-lab` — Hands-on admission control lab on a local kind/k3d cluster — enable built-in Pod Security Admission profiles per namespace in warn/audit before enforce, then add custom rules with Kyverno ClusterPolicies and with in-tree CEL ValidatingAdmissionPolicy, grant scoped exceptions, and run policy unit tests in CI so a bad manifest fails the pipeline instead of the cluster.
- `/k8s-autoscaling-lab` — Hands-on lab on Kubernetes autoscaling — build it step by step: set resource requests/limits, install metrics-server, create a Horizontal Pod Autoscaler, then generate load and watch replicas scale.
- `/k8s-cluster-lifecycle-lab` — Hands-on cluster operations lab — bootstrap a control plane with kubeadm init and join a worker, then rehearse the operations that keep it alive: a minor-version upgrade under the version skew policy, etcd snapshot backup and restore, cordon and drain with PodDisruptionBudgets, certificate expiry checks and renewal, and what changes in a stacked HA topology.
- `/k8s-configmap-secret-lab` — Hands-on lab on Kubernetes ConfigMaps and Secrets — inject config step by step: as env vars vs mounted files, refresh behavior, and the key safety truth that Secret data is base64-encoded, not encrypted.
- `/k8s-cost-optimization-lab` — Cut Kubernetes spend without cutting reliability — measure real usage, right-size requests and limits with arithmetic instead of superstition, understand why requests (not limits) drive cost and scheduling, use VPA recommendations and HPA correctly together, improve bin-packing and node shapes, exploit spot/preemptible capacity safely, cap tenants with ResourceQuota and LimitRange, and attribute spend per namespace with OpenCost.
- `/k8s-deployment-lab` — Hands-on lab on a Kubernetes Deployment — build one step by step: pods, replicas, label selectors, a rolling update, and a rollback, applying and observing each change with kubectl.
- `/k8s-network-policy-lab` — Hands-on NetworkPolicy lab on a local kind/k3d/minikube cluster with a policy-enforcing CNI — start from a default-deny baseline, add ingress and egress allow-lists with podSelector, namespaceSelector and ipBlock, fix the CoreDNS egress gotcha, and prove with curl from real pods that traffic is actually blocked rather than merely declared.
- `/k8s-operator-crd-lab` — Build a real Kubernetes operator on a free local cluster — design a CustomResourceDefinition with a spec/status split and OpenAPI validation, scaffold a controller with kubebuilder and controller-runtime, write a level-triggered idempotent reconcile loop that compares desired to observed state, own child objects with controller references, report progress with standard status conditions and observedGeneration, and handle deletion safely with finalizers.
- `/k8s-rbac-lab` — Hands-on RBAC lab on a local kind/k3d/minikube cluster — build least-privilege access from scratch with ServiceAccounts, Roles and ClusterRoles, RoleBindings and ClusterRoleBindings, projected bound service account tokens, then audit it with kubectl auth can-i --as and close the privilege-escalation paths opened by the escalate, bind and impersonate verbs.
- `/k8s-scheduling-lab` — Hands-on Kubernetes scheduling lab on a free local kind cluster — set requests and limits (and understand QoS, CPU throttling vs OOMKill), steer pods with node affinity, spread or co-locate them with pod affinity/anti-affinity, repel workloads with taints and tolerations, and balance across zones with topologySpreadConstraints, reading every decision back out of the scheduler's own events.
- `/k8s-service-networking-lab` — Hands-on lab on Kubernetes Services and networking — expose a workload step by step: ClusterIP vs NodePort vs LoadBalancer, in-cluster DNS, endpoints, and Ingress basics, testing each with kubectl.
- `/k8s-storage-lab` — Hands-on Kubernetes persistent-storage lab on a free local kind cluster — watch a PVC bind to a PV, use a StorageClass for dynamic provisioning, understand access modes (RWO / ROX / RWX / RWOP) and reclaim policies, feel the difference between Immediate and WaitForFirstConsumer binding, expand a volume, and give every pod its own disk with StatefulSet volumeClaimTemplates.
- `/k8s-troubleshooting-lab` — Hands-on lab that builds a systematic Kubernetes debugging reflex on a local kind/k3d/minikube cluster — deliberately break things, then triage CrashLoopBackOff, ImagePullBackOff, Pending and OOMKilled pods with describe/events/logs, ephemeral debug containers, kubelet and crictl on the node, and walk the Service → EndpointSlice → DNS chain until traffic flows again.
- `/kafka-connect-lab` — Hands-on Kafka Connect lab: move data without code — run a source and a sink connector, write connector config JSON, pick converters (JSON/Avro) for serialization, and drive it via the REST API.
- `/kafka-consumer-groups-lab` — Hands-on Kafka lab: scale consumers with consumer groups — group.
- `/kafka-consumer-lab` — Hands-on Kafka lab: build a consumer end to end — the poll loop, deserialization, offsets, and commit strategies (auto vs manual, sync vs async, at-least-once vs at-most-once).
- `/kafka-kraft-local-lab` — Hands-on lab running Apache Kafka in KRaft mode (no ZooKeeper) locally, free, no subscription via the official apache/kafka Docker image.
- `/kafka-producer-lab` — Hands-on Kafka lab: build a producer end to end — send records with the KafkaProducer API, choose keys and partitioning, tune acks (0/1/all), and turn on idempotence for duplicate-free delivery.
- `/kafka-streams-lab` — Hands-on Kafka Streams lab: build a topology — KStream vs KTable, stateless ops (map/filter) vs stateful (aggregate/join with state stores), and windowing (tumbling/hopping/session).
- `/kafka-topics-partitions-lab` — Hands-on Kafka lab: design topics and partitions — create topics, choose a partition count for parallelism, set replication factor and min.
- `/keycloak-auth-local-lab` — Hands-on lab: run Keycloak locally with Docker — create a realm, an OIDC client, and users, then practice an OAuth2/OIDC login flow.
- `/kind-lab` — Hands-on lab on kind (Kubernetes IN Docker) — build a free, local multi-node cluster with no subscription: create control-plane + worker nodes as Docker containers, load a locally-built image without a registry, deploy, and verify.
- `/kotlin-coroutines-flow-lab` — Hands-on Kotlin coroutines and Flow lab — structured concurrency and CoroutineScope, launch vs async, dispatchers, cooperative cancellation and NonCancellable cleanup, cold Flow vs hot StateFlow/SharedFlow, backpressure via buffer/conflate/collectLatest, and deterministic tests with runTest virtual time.
- `/kubernetes-security-hardening-lab` — Harden a throwaway local Kubernetes cluster (kind or k3d) in four defensive moves — enforce Pod Security Admission at the namespace, apply a default-deny NetworkPolicy and open only what is needed, cut RBAC down to least privilege, and turn on seccomp RuntimeDefault plus a non-root securityContext — proving each control by watching a deliberately bad manifest get rejected.
- `/kustomize-lab` — Hands-on Kustomize lab — build one base and layer dev/staging/prod overlays on it, choose correctly between strategic-merge and JSON6902 patches, generate ConfigMaps and Secrets whose name hash forces a rolling update, and render everything with `kubectl kustomize` before it ever reaches a cluster.
- `/linux-networking-lab` — Hands-on Linux networking lab: inspect interfaces and routes with ip, list listening ports and sockets with ss, test HTTP with curl, and troubleshoot connectivity layer by layer (link → IP → route → DNS → port).
- `/linux-permissions-lab` — Hands-on Linux lab on file permissions: read/write/execute (rwx) bits for user/group/other, chmod (symbolic and octal), chown/chgrp ownership, umask defaults, and special bits (setuid, setgid, sticky).
- `/linux-processes-lab` — Hands-on Linux lab on processes: inspect with ps and top, send signals with kill (SIGTERM vs SIGKILL), manage shell jobs (bg/fg/jobs/nohup), and set priority with nice/renice.
- `/linux-storage-lab` — Hands-on Linux storage lab: list disks and partitions with lsblk, format and mount filesystems, make mounts persistent in /etc/fstab, measure usage with df and du, and grow volumes with LVM basics (PV/VG/LV).
- `/linux-systemd-lab` — Hands-on Linux lab on systemd services: read unit files, start/stop/restart, enable at boot, check status, and read logs with journalctl (journald).
- `/linux-users-lab` — Hands-on Linux lab on users and groups: create and modify accounts with useradd/usermod, manage supplementary groups, grant sudo safely with visudo, and handle passwords and account aging with passwd/chage.
- `/llm-observability-lab` — Instrument an LLM application end to end with OpenTelemetry — one span per model call and per tool call, token and cost and latency as real metrics, prompts captured as events under a privacy switch, and online evals attached to the trace so a quality regression is debuggable.
- `/llm-quantization-lab` — Hands-on local lab for shrinking an open-weight LLM and measuring what you paid for it — load a small model in fp16, then in 8-bit and 4-bit NF4 with bitsandbytes, export and run GGUF quantizations (Q8_0, Q5_K_M, Q4_K_M) with llama.
- `/local-llm-agent-lab` — Hands-on local agent lab — build a small tool-using LLM agent against a local Ollama model with no API key, no subscription, and no cost, fully offline.
- `/localstack-lab` — Hands-on AWS lab: practice AWS locally and fully offline with LocalStack, the free open-source AWS emulator — no cloud account, no subscription, no real credentials.
- `/locust-load-test-lab` — Hands-on lab: write user-behavior load tests locally with Locust, the free Python OSS tool, and drive them from its web UI.
- `/loki-logging-local-lab` — Hands-on lab: run Grafana Loki locally with Docker — ship logs with Promtail, query with LogQL, and view them in Grafana.
- `/lwc-component-lab` — Hands-on Lightning Web Component lab in a free Salesforce Developer Edition org — build a component with @api/@wire/@track, choose between wire and imperative Apex, read and write records through Lightning Data Service instead of custom Apex, publish and subscribe over Lightning Message Service, and prove behaviour with sfdx-lwc-jest tests.
- `/mailpit-email-local-lab` — Hands-on lab: run Mailpit locally with Docker — a fake SMTP server plus web UI that captures test emails so no real mail is sent.
- `/manim-animation-lab` — Hands-on local lab for Manim Community Edition in Python — set up a free offline environment, write a Scene that animates an algorithm, a geometric transformation, or a proof step, render it to MP4 or GIF, and fall back to a static PNG frame when ffmpeg or LaTeX is missing.
- `/matplotlib-lab` — Hands-on lab on Matplotlib: create a Figure and Axes with the object-oriented API, draw line/bar/scatter/histogram plots, then customize labels, legends, ticks, and limits — learning by running real code.
- `/meilisearch-local-lab` — Hands-on Meilisearch lab — run the open-source (MIT) Meilisearch locally with Docker, free and offline, no subscription.
- `/metabase-local-lab` — Hands-on lab on running Metabase (OSS) locally: connect a database and build a dashboard — free, local business intelligence with no subscription.
- `/minikube-lab` — Hands-on lab on minikube — spin up a free, local single-node Kubernetes cluster with no subscription: start the cluster, deploy an app, expose it as a Service, and open it in a browser.
- `/minio-s3-lab` — Hands-on object-storage lab: practice the Amazon S3 API locally and fully offline with MinIO, free open-source S3-compatible storage — no AWS account, no subscription, no cloud keys.
- `/mobile-app-performance-lab` — Hands-on mobile performance lab that measures before it optimises — cold/warm/hot start time, jank and dropped frames against a 60/90/120 fps frame budget, memory footprint and terminations, and battery/network cost — using Instruments and XCTest metrics on iOS and Android Profiler, Macrobenchmark and JankStats on Android.
- `/mobile-ui-testing-lab` — Hands-on mobile UI test lab — write XCUITest on iOS, Espresso/Compose tests on Android, and cross-platform Appium suites, then make them fast and non-flaky with proper synchronisation (idling resources, waitForExistence, composeTestRule.
- `/model-explainability-lab` — Explain a model without fooling yourself — separate global from local explanations, compute SHAP and permutation importance and PDP/ICE correctly, verify the additivity identity yourself, and state out loud where each method breaks under correlated features.
- `/model-serving-lab` — Put a trained model behind an HTTP endpoint that survives production: choose between a FastAPI wrapper, ONNX Runtime, and Triton Inference Server, implement dynamic micro-batching, reason about the latency-versus-throughput trade with Little's Law, wire real liveness/readiness probes, and pick the autoscaling signal that actually tracks saturation.
- `/modern-css-features-lab` — Hands-on lab for the CSS that finally replaced the hacks — container queries with @container, the relational :has() selector, cascade layers with @layer, native nesting, subgrid, typed custom properties with @property, and color-mix() — each taught against the cascade rules that decide who actually wins.
- `/mongodb-local-lab` — Hands-on lab: run MongoDB locally with Docker — free, no subscription.
- `/mosquitto-mqtt-lab` — Hands-on lab running an Eclipse Mosquitto MQTT broker locally, free, no subscription via Docker (eclipse-mosquitto, port 1883).
- `/mpi-openmp-parallel-lab` — Build and run genuinely hybrid parallel code — MPI ranks across address spaces plus OpenMP threads inside each rank — compiled with mpicc -fopenmp and launched with mpirun, including MPI_Init_thread levels, collectives (Reduce/Allreduce/Bcast/Barrier), correct pinning, and a strong/weak scaling measurement.
- `/mysql-local-lab` — Hands-on lab: run MySQL locally with Docker — free, open-source, no subscription.
- `/n8n-local-lab` — Hands-on lab: run n8n (fair-code workflow automation) locally — free to self-host, no subscription.
- `/nats-local-lab` — Hands-on lab running NATS locally, free, no subscription via Docker (nats image, port 4222).
- `/newman-api-test-lab` — Hands-on lab: run a Postman collection of API tests locally with Newman, Postman's free CLI.
- `/nextflow-nfcore-lab` — Build and run reproducible Nextflow DSL2 pipelines and drive community nf-core workflows — processes and channels, operators, config profiles (docker/conda/slurm), publishDir, the work directory and -resume caching, plus the execution report/trace/DAG that proves what ran.
- `/nextjs-app-lab` — Hands-on Next.
- `/nginx-local-lab` — Hands-on lab: run NGINX locally to learn reverse proxying, static file serving, and location blocks — free, open-source, no subscription.
- `/node-exporter-lab` — Hands-on lab: run Prometheus node_exporter locally with Docker — expose host CPU, memory, disk, and filesystem metrics for Prometheus to scrape.
- `/numpy-lab` — Hands-on lab on NumPy: create ndarrays, use broadcasting to combine different shapes, replace loops with vectorized ops, reduce along axes, and measure the performance win — learning by running real code.
- `/oci-core-services-lab` — Hands-on Oracle Cloud Infrastructure lab for the four things everything else rests on: compartments as the isolation and quota boundary, the declarative IAM policy grammar (allow subject to verb resource-type in location where condition), the VCN with subnets, gateways, security lists and NSGs, and Object Storage tiers with their retention and retrieval trade-offs.
- `/ollama-local-llm-lab` — Hands-on Ollama lab — run open-weight LLMs locally on Mac/Windows/Linux with no API key, no subscription, and no cost, fully offline.
- `/ollama-rag-lab` — Hands-on local RAG lab — build a fully offline retrieval-augmented generation pipeline with Ollama plus a local embedding model (nomic-embed-text) and a local vector store, with no API key, no subscription, and no cost.
- `/opa-policy-lab` — Hands-on lab to run Open Policy Agent (OPA) locally as a defensive policy-as-code engine — write Rego rules, feed them JSON input, and evaluate allow/deny decisions for authorization, Kubernetes admission, and IaC/config checks, with unit tests.
- `/opensearch-local-lab` — Hands-on OpenSearch lab — run the Apache-2.
- `/opentelemetry-collector-lab` — Hands-on lab: run the OpenTelemetry Collector locally with Docker — build a receivers → processors → exporters pipeline for traces, metrics, and logs.
- `/owasp-zap-baseline-lab` — Hands-on lab to run the OWASP ZAP baseline scan locally against your OWN authorized web app — a passive, non-intrusive DAST pass that spiders the site and reports missing security headers, insecure cookie flags, and information disclosure without attacking it.
- `/pact-contract-lab` — Hands-on lab: run consumer-driven contract tests locally with Pact (free/OSS) and a local Pact Broker.
- `/pandas-lab` — Hands-on lab on pandas fundamentals: build a Series and DataFrame, select with loc/iloc, filter with boolean masks, and replace Python loops with vectorized operations — learning by running real code.
- `/peft-lora-lab` — Hands-on local lab for parameter-efficient fine-tuning — run a real LoRA and QLoRA supervised fine-tune on a small open-weight model with Hugging Face PEFT and TRL's SFTTrainer, choose rank r, alpha and dropout deliberately, pick target modules (attention projections vs.
- `/pgvector-local-lab` — Hands-on pgvector lab — add vector similarity search inside your local PostgreSQL with no API key, no subscription, and no cost, fully offline.
- `/playwright-test-lab` — Hands-on lab: build reliable end-to-end browser tests locally with Playwright, the free OSS framework.
- `/plotly-lab` — Hands-on lab on Plotly: build interactive charts with Plotly Express (hover, zoom, legend toggle), export to standalone HTML, then wire a minimal local Dash app with a dropdown and a callback — learning by running real code.
- `/polars-lab` — Hands-on Polars lab you can run locally for free — eager DataFrame vs lazy LazyFrame, the expression API and why expressions parallelise, group_by/agg, the join how= variants including semi/anti, scan_parquet with projection and predicate pushdown read from .
- `/postgis-spatial-lab` — Learn PostGIS by running it — geometry vs geography, why SRID 4326 distances come back in degrees, projecting to metres, GiST bounding-box indexes and how ST_DWithin uses them, plus real spatial joins and EXPLAIN ANALYZE proof.
- `/postgres-local-lab` — Hands-on lab: run PostgreSQL locally with Docker — free, open-source, no subscription.
- `/pre-commit-lab` — Hands-on lab to wire local Git hooks with the pre-commit framework (open-source, no subscription) — add a .
- `/prefect-local-lab` — Hands-on lab: run Prefect (open-source Python orchestration) locally — free, no subscription.
- `/progressive-delivery-lab` — Hands-on progressive delivery lab with Argo Rollouts on a local kind/k3d cluster — replace a Deployment with a Rollout, run a weighted canary with pause steps, run a blue-green release with a preview Service and manual promotion, wire an AnalysisTemplate so bad metrics auto-abort, and rehearse a mid-rollout abort and rollback.
- `/prometheus-grafana-local-lab` — Hands-on lab: run Prometheus + Grafana locally with Docker — scrape metrics from a target, query with PromQL, and build a dashboard.
- `/pulsar-local-lab` — Hands-on lab running Apache Pulsar standalone locally, free, no subscription via Docker (apachepulsar/pulsar, ports 6650/8080).
- `/python-asyncio-lab` — Hands-on Python lab on asyncio: coroutines with async/await, scheduling tasks, awaiting many with gather, the single-threaded event loop, and choosing async for IO-bound vs processes for CPU-bound work.
- `/python-cli-lab` — Hands-on Python lab on command-line interfaces: argparse (and click), positional arguments vs optional options/flags, type conversion and defaults, subcommands, and auto-generated --help.
- `/python-context-managers-lab` — Hands-on Python lab on context managers: the with statement, writing __enter__/__exit__, building one from a generator with contextlib.
- `/python-dataclasses-lab` — Hands-on Python lab on dataclasses: declaring fields, safe mutable defaults with field(default_factory), frozen immutable instances, __post_init__ validation, generated comparison/ordering, and when to pick namedtuple or pydantic instead.
- `/python-decorators-lab` — Hands-on Python lab on decorators: functions as first-class objects, wrapping a callable, preserving metadata with functools.
- `/python-generators-lab` — Hands-on Python lab on generators and iterators: the yield keyword, lazy evaluation, the iterator protocol, generator expressions, chaining generators into pipelines, and the memory wins over lists.
- `/python-logging-lab` — Hands-on Python lab on logging: the standard library logging module — loggers, handlers, and formatters, the DEBUG/INFO/WARNING/ERROR/CRITICAL levels, basicConfig vs dictConfig, per-module getLogger(__name__), and logger.
- `/python-multiprocessing-lab` — Hands-on Python lab on multiprocessing: Process and Pool, map/apply, sharing data with Queue/Pipe/Value/Array, and why the GIL means processes (not threads) give real CPU-bound parallelism.
- `/python-packaging-lab` — Hands-on Python lab on packaging: the pyproject.
- `/python-pattern-matching-lab` — Teach Python structural pattern matching as PEP 634 defines it — the six pattern kinds, why a bare name is a CAPTURE and not a comparison, sequence patterns that deliberately exclude str/bytes, mapping patterns that match partially, class patterns driven by __match_args__, guards, OR/AS patterns, and how to get exhaustiveness from a type checker since the runtime gives you none — then hand over a runnable module traced case by case.
- `/python-testing-lab` — Hands-on Python lab on testing with pytest: test_* functions, plain assert, fixtures for setup, @pytest.
- `/python-typing-lab` — Hands-on Python lab on type hints: annotating functions, Optional/Union, generics with TypeVar, structural typing with Protocol, and checking it all with mypy.
- `/python-venv-lab` — Hands-on Python lab on virtual environments: creating an isolated env with venv, installing with pip, pinning dependencies via requirements.
- `/pytorch-autograd-lab` — Hands-on PyTorch lab on autograd: set requires_grad, run a forward pass, call backward() to compute gradients, read .
- `/pytorch-dataloader-lab` — Hands-on PyTorch lab on Dataset & DataLoader: write a custom Dataset with __len__/__getitem__, batch and shuffle with DataLoader, and apply transforms — learning by running real code.
- `/pytorch-nn-module-lab` — Hands-on PyTorch lab on nn.
- `/pytorch-tensors-lab` — Hands-on PyTorch lab on tensors: create tensors, inspect shape/dtype, reshape, broadcast, move between CPU and GPU (device), and run element-wise & matmul ops — learning by running real code.
- `/pytorch-training-loop-lab` — Hands-on PyTorch lab on the training loop: forward pass, compute loss, zero_grad, backward, optimizer.
- `/pytorch-transfer-learning-lab` — Hands-on PyTorch lab on transfer learning: load a pretrained torchvision model, freeze the backbone, replace the classifier head, and fine-tune on a new task — learning by running real code.
- `/qdrant-local-lab` — Hands-on Qdrant lab — run the open-source Qdrant vector database locally via Docker with no API key, no subscription, and no cost, fully offline.
- `/qiskit-circuits-lab` — Hands-on Qiskit 2.
- `/query-plan-tuning-lab` — Hands-on local lab for reading and fixing query plans — run PostgreSQL (and MySQL) free in Docker, seed realistic data, then use EXPLAIN (ANALYZE, BUFFERS) to identify scan and join node types, spot row misestimates (estimated vs actual), read shared hit/read buffers, fix stale statistics with ANALYZE and n_distinct, and benchmark a query before and after an index with real timings.
- `/rabbitmq-local-lab` — Hands-on lab running RabbitMQ locally, free, no subscription via Docker (rabbitmq:management, ports 5672/15672).
- `/react-context-lab` — Hands-on React lab on Context — createContext, providing and consuming with useContext, killing prop drilling, and taming re-render pitfalls — by refactoring a prop-drilled tree.
- `/react-forms-lab` — Hands-on React lab on forms — controlled vs uncontrolled inputs, validation, submission with form actions, and accessible error handling — by building a validated, submittable form.
- `/react-hooks-lab` — Hands-on React lab on core hooks — useState, useEffect, useRef, useMemo, useCallback, dependency arrays, and the Rules of Hooks — by building and fixing one small component.
- `/react-native-new-architecture-lab` — Hands-on React Native New Architecture lab — JSI instead of the async bridge, the Fabric renderer, TurboModules with Codegen, bridgeless mode (default since React Native 0.
- `/react-performance-lab` — Hands-on React lab on performance — memo, useMemo, useCallback, stable keys, list virtualization, and Profiler-driven measurement — by finding and fixing needless re-renders.
- `/react-server-components-lab` — Hands-on React lab on Server Components with the Next.
- `/react-suspense-lab` — Hands-on React lab on Suspense and loading UX — Suspense boundaries, React.
- `/redis-local-lab` — Hands-on lab: run Redis locally with Docker — free, no subscription.
- `/redpanda-local-lab` — Hands-on lab running Redpanda locally, free, no subscription via Docker — a Kafka-API-compatible streaming broker as a single binary with no ZooKeeper and no JVM.
- `/ros2-nodes-lab` — Hands-on ROS 2 lab on a free local install — write rclpy nodes with publishers and subscribers, pick the right QoS profile so topics actually connect, choose between topics, services and actions, declare parameters, wire it together with a Python launch file, and debug live with ros2 topic/node/param and ros2 bag.
- `/ros2-sim-nav-lab` — Drive a simulated robot end to end with ROS 2 and Nav2 — nodes, topics and the DDS graph, Python launch files, Gazebo simulation through ros_gz, then the navigation stack itself (map → AMCL → costmaps → planner → controller → behaviour tree) with real goal-sending and debugging.
- `/rust-async-lab` — Hands-on Rust lab on async: lazy futures, async/await, the .
- `/rust-borrowing-lab` — Hands-on Rust lab on borrowing and references: shared &T vs mutable &mut T borrows, the borrow-checker rules, and non-lexical lifetimes.
- `/rust-error-handling-lab` — Hands-on Rust lab on error handling: Result and the ? operator, Option, and custom error types (the thiserror concept).
- `/rust-lifetimes-lab` — Hands-on Rust lab on lifetimes: generic lifetime annotations like 'a, the elision rules, 'static, and why lifetimes prevent dangling references.
- `/rust-ownership-lab` — Hands-on Rust lab on ownership: move semantics, Copy vs Clone, the three ownership rules, and values dropped at end of scope.
- `/rust-smart-pointers-lab` — Teach Rust's smart pointers as a decision about OWNERSHIP, sharing and mutability — Box for single heap ownership and recursive types, Rc/Arc for shared ownership (non-atomic vs atomic counts), RefCell/Mutex/RwLock for interior mutability with runtime borrow checks, Weak to break reference cycles that Rc would otherwise leak, and how Deref coercion and Drop order actually behave — then hand over a compilable crate whose reference counts are printed and traced.
- `/rust-traits-lab` — Hands-on Rust lab on traits: defining and implementing traits, default methods, trait bounds, and static (generics / impl Trait) vs dynamic (dyn) dispatch.
- `/schema-registry-lab` — Hands-on local lab that puts a real contract on a Kafka topic — run Confluent Schema Registry (or Karapace) in Docker for free, register Avro/Protobuf/JSON Schema subjects, set the compatibility mode, decode the 5-byte wire format by hand, watch a breaking change get rejected, and wire a compatibility check into CI.
- `/seaborn-lab` — Hands-on lab on Seaborn: set a theme, plot distributions (histplot/kdeplot/ecdfplot) and statistical views (boxplot/violinplot/regplot), and map categories with hue and facets on tidy DataFrames — learning by running real code.
- `/semgrep-lab` — Hands-on lab to run Semgrep OSS locally as a defensive static-analysis (SAST) scanner — find security bug patterns, injection, hardcoded secrets, and dangerous API use in your OWN source code, and author custom pattern rules in YAML.
- `/service-mesh-lab` — Hands-on service mesh on a free local cluster — see what a sidecar or ambient data plane actually does, turn on automatic mTLS and prove it with STRICT PeerAuthentication, shift traffic for a canary, add timeouts, retries and outlier detection, inject L7 faults, and weigh the real cost of a mesh (latency, memory, upgrade risk, debugging) against doing it in libraries or at the gateway.
- `/servicenow-glide-scripting-lab` — Hands-on ServiceNow server-side scripting lab on a free Personal Developer Instance — query with GlideRecord and encoded queries, write business rules that run in the right phase, move logic into Script Includes, call them from the client with GlideAjax instead of client-side GlideRecord, package work in a scoped application, and secure it with ACLs.
- `/sklearn-classification-lab` — Hands-on lab: train and evaluate a scikit-learn classifier — fit LogisticRegression/RandomForest, read a confusion matrix, and compute precision, recall, and F1 with classification_report.
- `/sklearn-clustering-lab` — Hands-on lab: run scikit-learn k-means, choose k with the elbow and silhouette methods, and evaluate clusters — scaling first because k-means uses Euclidean distance.
- `/sklearn-model-selection-lab` — Hands-on lab: split train/test, run k-fold cross-validation, and tune hyperparameters with GridSearchCV — estimating generalization honestly without leaking the test set.
- `/sklearn-pipelines-lab` — Hands-on lab: compose a scikit-learn Pipeline with ColumnTransformer so preprocessing fits inside cross-validation and never leaks the test set.
- `/sklearn-preprocessing-lab` — Hands-on lab: preprocess features in scikit-learn the right way — scale numerics (StandardScaler), encode categoricals (OneHotEncoder), and impute missing values (SimpleImputer) fit on training data only.
- `/sklearn-regression-lab` — Hands-on lab: fit a scikit-learn regressor and evaluate it honestly — compute R², MAE, and RMSE and read a residual plot to spot bias and heteroscedasticity.
- `/slurm-hpc-job-lab` — Run real Slurm workloads on a free single-node cluster you build yourself — sbatch batch scripts, srun job steps, salloc interactive allocations, partitions and QOS, job arrays, correct CPU/memory/time requests, and sacct/seff accounting to prove what the job actually consumed.
- `/solr-local-lab` — Hands-on Apache Solr lab — run the open-source (Apache-2.
- `/sonarqube-local-lab` — Hands-on lab to run SonarQube Community Build (free, open-source, no subscription) locally in Docker and scan a project with SonarScanner — surface bugs, code smells, vulnerabilities, duplications, and a quality gate at localhost:9000.
- `/spark-dataframe-lab` — Hands-on lab on Spark DataFrames: build a DataFrame with an explicit schema, then select/filter/withColumn and read the Catalyst optimizer's plans via explain() — learning PySpark by running real code.
- `/spark-partitioning-lab` — Hands-on lab on Spark partitioning: compare repartition vs coalesce, diagnose data skew from straggler tasks, and prove partition pruning on a partitioned table — learning PySpark performance by running real code.
- `/spark-rdd-lab` — Hands-on lab on Spark RDDs: create RDDs with parallelize/textFile, chain transformations (map/filter/flatMap) vs.
- `/spark-sql-lab` — Hands-on lab on Spark SQL: register a DataFrame as a temp view, run SQL queries with spark.
- `/spark-streaming-lab` — Hands-on lab on Spark Structured Streaming: build a readStream, pick an output mode (append/update/complete), bound state with watermarks, and control micro-batches with triggers — learning PySpark streaming by running real code.
- `/spark-transformations-lab` — Hands-on lab on Spark transformations: tell narrow (map/filter/flatMap) from wide (reduceByKey/groupByKey/join) ops, build a word count, and see exactly where a shuffle and stage boundary appear — learning PySpark by running real code.
- `/spring-boot-lab` — Hands-on Spring Boot lab: build a REST controller, wire beans with dependency injection, and externalize configuration.
- `/sql-aggregation-lab` — Hands-on lab on SQL aggregation: GROUP BY, filtering groups with HAVING, and multi-level subtotals with GROUPING SETS, ROLLUP, and CUBE — learning by writing real summary queries.
- `/sql-cte-lab` — Hands-on lab on SQL CTEs: the WITH clause, chaining multiple CTEs, and recursive CTEs (WITH RECURSIVE) for hierarchies and graphs — learning by building a complex query in readable steps.
- `/sql-indexing-lab` — Hands-on lab on SQL indexing: create indexes for a query's filters and sorts, read an EXPLAIN plan, and build composite and covering indexes for index-only scans — learning by speeding up a real slow query.
- `/sql-joins-lab` — Hands-on lab on SQL joins: INNER, LEFT, RIGHT, and FULL OUTER joins, self-joins, and avoiding fan-out (row multiplication) — learning by writing and running real queries.
- `/sql-subqueries-lab` — Hands-on lab on SQL subqueries: scalar subqueries, correlated subqueries, EXISTS vs IN, and when a JOIN is clearer or faster — learning by writing and rewriting real nested queries.
- `/sql-window-functions-lab` — Hands-on lab on SQL window functions: OVER and PARTITION BY, ranking (ROW_NUMBER/RANK/DENSE_RANK), running totals, and LAG/LEAD — learning by writing real analytic queries.
- `/sqlite-lab` — Hands-on lab: use SQLite locally — free, open-source, no subscription, no server, zero setup.
- `/statsmodels-lab` — Hands-on lab on statsmodels: fit an OLS regression with the formula API, read the summary() table (coefficients, p-values, R-squared, confidence intervals), and run diagnostics (residual and QQ plots, Breusch-Pagan, VIF, Durbin-Watson) — learning by running real code.
- `/streamlit-local-lab` — Hands-on lab building a Streamlit data app locally: understand the top-to-bottom rerun model, add widgets (slider/selectbox) that filter a DataFrame, render a reactive chart, and cache data with @st.
- `/supply-chain-attestation-lab` — Hands-on software supply-chain attestation, entirely local and free — understand SLSA build levels and what provenance actually proves, read the in-toto Statement/predicate envelope, sign and verify artifacts with sigstore cosign (key-based offline and keyless with Fulcio/Rekor), attach SLSA provenance and SBOM attestations to an image in a local registry, then verify them and watch a tampered artifact fail.
- `/svelte-basics-lab` — Hands-on Svelte 5 lab — reactivity with runes ($state/$derived), components, props via $props, and shared stores — by building one small app.
- `/svelte-runes-lab` — Hands-on lab for Svelte 5 runes — $state deep proxies, lazy memoized $derived, post-DOM $effect, $props with $bindable, and universal reactivity moved out of components into .
- `/swagger-ui-local-lab` — Hands-on lab: render and try an OpenAPI spec locally with Swagger UI or Redoc — free, open-source, no subscription.
- `/swift-concurrency-lab` — Hands-on Swift concurrency lab — async/await, structured tasks and task groups, cancellation, actors and @MainActor isolation, Sendable, and adopting Swift 6 compile-time data-race safety incrementally through the strict-concurrency settings.
- `/swiftui-lab` — Hands-on SwiftUI lab that builds one real screen from first principles — view identity, @State/@Binding/@Observable ownership, the layout negotiation, NavigationStack with a bound path, stable list identity and performance, previews as the feedback loop, and Dynamic Type / Dark Mode checks.
- `/synthetic-data-lab` — Generate synthetic data and — the hard part — prove it is good enough: tabular synthesis with SDV/CTGAN or a Gaussian copula, LLM-generated text datasets, and augmentation, then score the three-way trade-off of fidelity vs utility vs privacy with KS/correlation checks, train-on-synthetic-test-on-real (TSTR), and distance-to-closest-record.
- `/tailwind-lab` — Hands-on Tailwind CSS lab — utility-first classes, responsive breakpoint prefixes, state variants, and theme customization — by styling one small UI.
- `/tempo-tracing-local-lab` — Hands-on lab: run Grafana Tempo locally with Docker — store traces sent over OTLP and query them in Grafana with TraceQL.
- `/temporal-local-lab` — Hands-on lab: run Temporal (open-source durable execution) locally — free, no subscription.
- `/terraform-basics-lab` — Hands-on Terraform lab: write your first configuration end to end — a provider, one resource, typed input variables and outputs, then terraform init/fmt/validate/plan/apply.
- `/terraform-import-lab` — Hands-on Terraform lab: bring existing (click-ops) resources under management — the import block vs terraform import CLI, finding resource IDs, generating configuration, and reconciling until plan is clean.
- `/terraform-modules-lab` — Hands-on Terraform lab: author and call a reusable module — child vs root modules, typed inputs and outputs, source paths, version pinning, and reuse across environments.
- `/terraform-provisioners-lab` — Hands-on Terraform lab: use provisioners as a last resort — local-exec/remote-exec/file, connection blocks, creation vs destroy-time, on_failure — and the lifecycle meta-argument (create_before_destroy, prevent_destroy, ignore_changes).
- `/terraform-state-lab` — Hands-on Terraform lab: understand state end to end — local vs remote backends (S3+DynamoDB, azurerm, gcs), state locking, and detecting/reconciling drift with plan -refresh-only.
- `/terraform-testing-lab` — Test Terraform properly on your own machine — write native `terraform test` files (.
- `/terraform-workspaces-lab` — Hands-on Terraform lab: use CLI workspaces for multiple environments — workspace new/select/list, per-workspace state, the terraform.
- `/testcontainers-lab` — Hands-on lab on Testcontainers — write integration tests that spin up disposable, local Docker service containers (Postgres, Redis, Kafka) with no subscription, in Java, Python, Go, or Node: start a real dependency, run tests against it, and let it auto-clean.
- `/timeseries-analysis-lab` — Hands-on lab on time series in pandas: build a DatetimeIndex, resample to change frequency, smooth with rolling windows, and lag with shift/diff — learning by running real code.
- `/traefik-local-lab` — Hands-on lab: run Traefik locally to learn dynamic routing with the Docker provider and middlewares — free, open-source, no subscription.
- `/transactional-outbox-lab` — Hands-on local lab that fixes the dual-write problem — write the business row and an outbox row in ONE local transaction, then publish to Kafka either by polling the outbox or by log-tailing the WAL with Debezium CDC, running entirely free in Docker (Postgres + Kafka in KRaft + Kafka Connect/Debezium).
- `/trino-local-lab` — Hands-on lab on running Trino locally with Docker: federated SQL that joins across multiple data sources with one engine — free, local, OSS, and no subscription.
- `/trivy-scan-lab` — Hands-on lab to run Trivy locally as a defensive scanner — find known vulnerabilities (CVEs), misconfigurations, exposed secrets, and license issues in your OWN container images, filesystems, Git repos, and IaC (Terraform/Dockerfile/Kubernetes).
- `/ts-declaration-files-lab` — Hands-on lab on TypeScript declaration files (.
- `/ts-decorators-lab` — Hands-on lab on TypeScript decorators and metadata — class and method decorators, the current Stage 3 standard vs legacy experimentalDecorators, decorator context, and practical uses.
- `/ts-generics-lab` — Hands-on lab on TypeScript generics — generic functions and classes, constraints (extends), default type parameters, and type inference.
- `/ts-mapped-types-lab` — Hands-on lab on TypeScript mapped and conditional types — key remapping with as, template literal types, conditional types, and inference with infer.
- `/ts-narrowing-lab` — Hands-on lab on TypeScript narrowing and type guards — typeof and instanceof, discriminated unions, user-defined type guards (x is T), and exhaustiveness with never.
- `/ts-utility-types-lab` — Hands-on lab on TypeScript utility types — Partial, Pick, Omit, Record, Readonly, and ReturnType, plus composing them into new types.
- `/typesense-local-lab` — Hands-on Typesense lab — run the open-source (GPLv3) Typesense locally with Docker, free and offline, no subscription.
- `/vault-local-lab` — Hands-on lab to run HashiCorp Vault in dev mode locally as a defensive secrets manager — store and read static KV secrets, then generate short-lived dynamic secrets with leases/TTL so credentials stop living in code or env files.
- `/victoriametrics-local-lab` — Hands-on lab: run VictoriaMetrics (single-node OSS) locally with Docker — scrape and ingest metrics, then query them with MetricsQL in the built-in vmui.
- `/view-transitions-lab` — Hands-on lab for the View Transition API — animate a DOM change with document.
- `/vision-language-model-lab` — Run a vision-language model on your own laptop, free and CPU-only — do CLIP zero-shot image classification from nothing but label strings, reproduce the logits by hand from normalised image/text embeddings and the learned temperature, see why prompt templates change the answer, caption an image with a small BLIP/LLaVA-style model, ground a text phrase to a box with open-vocabulary detection, and learn where these models reliably fail (counting, spatial relations, compositionality).
- `/vite-lab` — Hands-on Vite lab — the native-ESM dev server, hot module replacement, production builds, and vite.
- `/vllm-inference-lab` — Hands-on lab for self-hosting an open-weight LLM with vLLM and understanding why it is fast — start the OpenAI-compatible server locally, learn PagedAttention and why KV-cache fragmentation was the bottleneck, watch continuous batching turn concurrency into throughput, enable prefix caching for shared system prompts, try speculative decoding, tune gpu-memory-utilization and max-model-len, then measure TTFT, inter-token latency, tokens/sec and throughput under real concurrency instead of trusting a README number.
- `/vue-basics-lab` — Hands-on Vue 3 lab — reactivity with ref/reactive, the Composition API and <script setup>, components, props, and custom events — by building one small app.
- `/weaviate-local-lab` — Hands-on Weaviate lab — run the open-source (BSD-3) Weaviate vector database locally with Docker, free and offline, no subscription.
- `/web-components-lab` — Hands-on lab for the browser's native component model — define a custom element and its lifecycle callbacks, encapsulate with shadow DOM, compose light DOM through slots, server-render with Declarative Shadow DOM, and make it a real form control with formAssociated plus ElementInternals for value, validity and ARIA.
- `/webgpu-compute-lab` — Run real GPU compute in the browser with WebGPU and WGSL — adapter/device request, storage buffers and usage flags, bind groups and layouts, workgroup sizing, dispatchWorkgroups, and the copy-to-staging + mapAsync readback that beginners always get wrong.
- `/whisper-speech-lab` — Transcribe speech locally and for free with OpenAI Whisper or faster-whisper — pick a model size from an explicit accuracy/latency budget, get segment and word-level timestamps, handle multilingual audio and the translate task, understand the fixed 30-second window and how long files are chunked, measure real Word Error Rate with proper text normalisation, and defuse the classic failure modes (hallucinated text over silence, repetition loops, wrong language detection).
- `/wiremock-api-mock-lab` — Hands-on lab: run WireMock locally with Docker — mock and stub HTTP APIs with request matching, canned responses, and record/playback.
- `/zephyr-rtos-lab` — Hands-on Zephyr RTOS through the official west workflow — initialise a workspace, describe hardware in devicetree, choose software in Kconfig, then build and actually run threads, workqueues and GPIO on QEMU or native_sim with no hardware to buy.

### Learn & understand  `(12)`

- `/analogy-generator` — Generate several strong analogies and metaphors for a hard concept, each mapped element-by-element to the real thing, with a note on where the analogy breaks down.
- `/cheat-sheet` — Produce a dense, well-organized one-page reference for a topic — syntax, common commands, key APIs, patterns, and gotchas — grouped logically and accurate, with sources/dates for version-specific facts.
- `/concept-explainer` — Explain any topic from first principles with analogies, worked examples, a diagram, and a check for understanding, adapted to the learner's level.
- `/glossary-builder` — Build a clear, alphabetized glossary of the key terms in a domain — concise, level-appropriate definitions with a quick example and 'see also' links between related terms.
- `/knowledge-graph` — Build and query a personal knowledge graph of concepts and how they connect (prerequisites, related, part-of, applied-in), stored as linked Markdown notes and/or a Mermaid diagram, to sequence learning and surface connections.
- `/mind-map` — Generate a mind map of a topic as a Mermaid diagram (plus a short outline) showing the central idea, its main branches, sub-concepts, and how they connect.
- `/misconception-buster` — Surface and correct the common misconceptions on a topic — list the frequent wrong mental models, explain why each is tempting, give the correct model with a disconfirming example, and a quick check.
- `/note-generator` — Turn a topic, lesson, or source into clean, structured study notes — summary, key points, a worked example, a diagram, common pitfalls, self-test questions, and links — using a consistent template.
- `/socratic-tutor` — Teach a topic purely through Socratic questioning — ask a ladder of guiding questions, react to the learner's answers, and lead them to derive the idea themselves, only confirming or correcting rather than lecturing up front.
- `/teach-back` — Run the Feynman technique — the learner explains a concept back in their own words while you find the gaps, ask probing questions, and correct misconceptions until they can teach it simply.
- `/visual-explainer` — Turn a concept into the RIGHT visual — pick from the full palette (Mermaid flowchart/sequence/class/ER/state/mindmap/timeline/journey/pie/quadrant/xychart/gitGraph/sankey/C4/requirement, KaTeX math, comparison tables, ASCII traces & number lines, and data charts) matched to what the idea is about, then render it correctly.
- `/worked-example` — Teach by fully working ONE representative problem end to end — restate it, plan the approach, solve step by step showing the reasoning at each step, verify, then generalize the pattern and give a similar exercise to try.

### Mobile  `(8)`

- `/android-lifecycle-coach` — Teach the Android activity, fragment, and Compose lifecycle as a lesson — lifecycle states, configuration changes, ViewModel, and lifecycle-aware collection.
- `/ios-lifecycle-coach` — Teach the iOS app and view lifecycle as a lesson — app states, SwiftUI view updates, scene phases, and where to run effects.
- `/mobile-accessibility-coach` — Teach and fix mobile accessibility on iOS and Android — VoiceOver and TalkBack navigation, semantic labels/roles/traits, focus order and grouping, 44pt/48dp touch targets, Dynamic Type and Android font scaling, colour contrast, reduce-motion, and automated audits with Accessibility Inspector, Accessibility Scanner and Espresso checks.
- `/mobile-app-security-coach` — Run a structured, DEFENSIVE, authorised-only security review of a mobile app YOU own — scope and authorisation first, then an OWASP MASVS/MASTG-guided assessment: data-at-rest in the app sandbox, data-in-transit and TLS validation, secrets and configuration in your own build artifacts, insecure logging and other leak paths, dependency and SDK risk, a static-then-dynamic analysis plan on devices you control, and a severity-ranked report with remediation and retest.
- `/mobile-offline-sync-coach` — Design offline-first mobile apps as a lesson — local store, background sync, conflict resolution, and an outbox queue on iOS and Android.
- `/mobile-push-notifications-coach` — Implement mobile push notifications as a lesson — APNs and FCM, device tokens, runtime permissions, notification channels, and payloads on iOS and Android.
- `/mobile-release-coach` — Ship a mobile app to the stores as a lesson — code signing, App Store/Play review guidelines, phased and staged rollout, and versioning on iOS and Android.
- `/mobile-security-coach` — Teach mobile security fundamentals for apps you build — the iOS/Android sandbox and permission models, secure local storage with the iOS Keychain and Android Keystore, transport security (ATS, Network Security Config, TLS and certificate/public-key pinning), biometric authentication done as a key gate rather than a boolean, what jailbreak/root detection can and cannot buy you, and the OWASP MASVS control areas.

### Networking & OS  `(7)`

- `/dns-coach` — Teach DNS and troubleshoot it: record types (A/AAAA/CNAME/MX/TXT/NS), the recursive resolution flow, TTL/caching, and diagnosing propagation and lookup failures with dig/nslookup.
- `/linux-command-coach` — Explain or build a Linux command or pipeline step by step: what each flag does, safer alternatives, and how to compose tools (pipes, redirection, xargs, find) — with a loud warning before destructive commands.
- `/load-balancing-coach` — Design load balancing from first principles: L4 vs L7, algorithms (round-robin, least-connections, hashing), health checks, session affinity/stickiness, TLS termination, and failover.
- `/networking-fundamentals-coach` — Teach TCP/IP networking from first principles: the layers, IP addresses & subnets/CIDR, TCP vs UDP, ports & sockets, NAT, and how a request flows end to end.
- `/os-internals-coach` — First-principles coach for operating-system internals: processes vs threads, what a context switch actually costs, how schedulers pick the next runnable task (Linux CFS then EEVDF, priorities, preemption), virtual memory, page tables, TLBs and page faults, the syscall and user/kernel boundary, and the buffered vs direct vs async I/O paths.
- `/shell-scripting-coach` — Write and teach a robust shell script: strict mode (set -euo pipefail), quoting, error handling/traps, idempotency, and portability (bash vs POSIX sh vs PowerShell) — with safety notes.
- `/tls-ssl-explainer` — Explain TLS/HTTPS defensively: the handshake, certificates & chains of trust, cipher suites, SNI, and common errors (expired, self-signed, hostname mismatch) — without ever bypassing validation.

### Plan & track  `(13)`

- `/career-ladder` — Map a technical career ladder and the concrete gaps to the next level (junior → mid → senior → staff/principal) — expected competencies per level, an honest placement of where the learner is now, and the specific moves to level up.
- `/cloud-cert-roadmap-coach` — Pick the right CURRENT cloud certification and sequence it — map role to cert across Azure/AWS/Google Cloud, verify a code is still live before you study (2025-26 retirements moved several targets), follow prerequisite chains, and budget cost, study hours, and renewal cadence.
- `/learner-memory` — Maintain a persistent learner memory so nothing is forgotten across sessions — create/read/update a `learning-profile.
- `/learner-onboarding` — First-run experience for a new learner — welcome them, interview to build their `learning-profile.
- `/learning-resource-finder` — Recommend the best FREE learning resources for a topic — YouTube channels/playlists, MOOCs (freeCodeCamp, CS50, MIT OpenCourseWare, fast.
- `/learning-roadmap` — Generate a dated, personalized learning roadmap or skill tree (30/60/90-day or custom) with phases, weekly and daily goals, milestones, curated resources, checkpoints, and a way to track progress.
- `/onboarding-plan` — Build a structured 30/60/90-day plan to onboard onto a new team, codebase, or technology — what to read, who to ask, what to build first, and checkpoints per phase.
- `/progress-charts` — Visualize a learner's progress as real charts using the Flint-Chart MCP server — render an activity/streak trend, topics completed by domain, mastery/coverage, and a spaced-repetition review burn-down from `learning-profile.
- `/progress-tracker` — Track learning progress over time — record completed topics, quiz scores, streaks, and due reviews into a simple Markdown log, then summarize momentum and what to do next.
- `/project-mentor` — Scope and guide a hands-on, portfolio-worthy project end to end — define goals and milestones, break it into steps with acceptance criteria, review progress, and reflect — so the learner builds real skills and something to show.
- `/roadmap-builder` — Build an outcome-based now/next/later roadmap instead of a dated feature list — themes tied to measurable outcomes, explicit confidence bands that widen with horizon, what you are deliberately NOT doing, and a stakeholder communication plan that survives the 'so when exactly?' question.
- `/session-resume` — Rich 'welcome back' that resumes a learner exactly where they left off — read `learning-profile.
- `/spaced-repetition-scheduler` — Manage a spaced-repetition review schedule for flashcards or topics — compute next due dates (expanding intervals like 1, 3, 7, 16, 35 days, or an SM-2-style ease factor), record results, and tell the learner what's due today and what's coming.

### Practice & assess  `(11)`

- `/aws-saa-exam-drill` — Timed scenario drills for the AWS Certified Solutions Architect – Associate exam, sampled to the published domain weights (Secure ~30%, Resilient ~26%, High-Performing ~24%, Cost-Optimized ~20%) and scored with a distractor-elimination protocol: find the constraint keyword, kill the two wrong answers first, then choose between the survivors.
- `/az-104-exam-drill` — Timed drills for Exam AZ-104 (Microsoft Azure Administrator) sampled to the current Skills Measured outline — identities and governance, storage, compute, virtual networking, monitoring — and drilled four ways at once: portal, Azure CLI, Azure PowerShell, and Bicep, because AZ-104 tests the operation rather than one tool.
- `/exam-blueprint` — Turn a certification or exam's official objective domains and weights into a study blueprint — list domains with weights (citing the vendor page, never guessing), map each to topics and resources, and suggest a time split.
- `/flashcards` — Create spaced-repetition flashcards for a topic — concise Q/A pairs and cloze-deletion cards, Anki-style, exportable to CSV/TSV or Markdown, with a suggested review schedule.
- `/gap-analysis` — Compare a learner's current skills against a target role or goal and produce a prioritized gap report — required competencies, current vs target level per skill, biggest/highest-leverage gaps first, and a plan to close them.
- `/gcp-ace-exam-drill` — Timed drills for the Google Cloud Associate Cloud Engineer exam, sampled to the published exam-guide sections and scored on gcloud recall — because ACE tests whether you can produce the right command, the right IAM binding, and the right billing or quota action, not whether you recognise a diagram.
- `/mock-exam` — Run a full, timed mock exam for a certification or subject with a realistic item mix, then score by objective domain, explain every answer, and produce a targeted study plan for the gaps.
- `/practice-generator` — Generate graded hands-on exercises, coding challenges, and mini-projects with clear acceptance criteria, progressive hints, starter scaffolding, and a reference solution.
- `/quiz-generator` — Create an adaptive quiz or mock exam with an answer key and explanations.
- `/rubric-grader` — Grade a learner's answer, essay, code, or artifact against an explicit rubric — confirm criteria and weights, score each with quoted evidence, give actionable feedback, and return a weighted total with next steps.
- `/skill-assessment` — Place a learner's level on a topic or role with a short adaptive diagnostic — ask graded questions easy→hard, infer Beginner/Intermediate/Advanced, and output a strengths/gaps map plus a recommended starting point.

### Product & process  `(15)`

- `/business-case-writer` — Write a business case that justifies an initiative — problem and drivers, options against a do-nothing baseline, costs vs benefits with ROI/NPV/payback, risks, and a clear recommendation.
- `/competitive-analysis-coach` — Run a structured competitive and market analysis — define the space and job-to-be-done, compare rivals on criteria the buyer values, read Porter's five forces, and find a gap to position against.
- `/engineering-metrics-coach` — Choose, instrument, and interpret engineering metrics using the DORA four keys plus the SPACE and DevEx frameworks (Forsgren, Storey, Noda et al.
- `/feature-prioritization-coach` — Prioritize a backlog with a real framework — RICE, ICE, or value-vs-effort — score items honestly against a goal, then sequence by dependency and risk.
- `/kanban-flow-coach` — Make work flow using queueing theory rather than heroics — set WIP limits, read a cumulative flow diagram, compute cycle time and throughput, apply Little's Law correctly, measure flow efficiency, and forecast with Monte Carlo percentiles instead of story-point estimates.
- `/metrics-definition-coach` — Define product metrics and KPIs that drive the right behavior — one North Star, supporting metrics via HEART or AARRR, guardrail/counter-metrics, and a vanity-metric check.
- `/okr-coach` — Set OKRs that drive outcomes — one inspiring objective plus 3–5 measurable key results, aligned to strategy and honest about ambition vs.
- `/one-on-one-coach` — Prepare an effective 1:1 — as manager or report — with an agenda, growth and feedback topics, strong open questions, and clear follow-ups, to build trust rather than status theater.
- `/prd-writer` — Draft a concise, testable product requirements document (PRD) — problem, goals and non-goals, target users, requirements, success metrics, and open questions.
- `/pricing-strategy-coach` — Design pricing and packaging — pick a value metric, choose a model (subscription, usage-based, tiered), set good-better-best tiers, apply price psychology honestly, and test willingness-to-pay with real research.
- `/product-discovery-coach` — Turn a fuzzy request into a Teresa Torres Opportunity Solution Tree — one measurable outcome, a mapped opportunity space grounded in real customer evidence, competing solutions per opportunity, and the smallest assumption test that could kill each one.
- `/retrospective-facilitator` — Facilitate an agile retrospective that leads to change — pick a format, gather data (what went well / didn't / to try), cluster themes to root cause, and produce a few owned, time-bound action items.
- `/stakeholder-management-coach` — Manage stakeholders on a project — map them on a power/interest grid, tailor communication and cadence, align on shared goals with RACI, and handle conflict by interests not positions.
- `/standup-summarizer` — Turn raw updates into a crisp standup or status report — yesterday, today, and blockers per person, with risks surfaced and filler removed.
- `/user-story-writer` — Write clear user stories with acceptance criteria — role-goal-benefit format, INVEST quality checks, and Given/When/Then criteria, splitting stories that are too big.

### Research & news  `(8)`

- `/daily-digest` — Produce a daily or weekly news digest for the learner's stack from authoritative sources — official releases and changelogs, vendor engineering blogs, communities, RSS/Atom feeds, curated newsletters, and papers — with a cited TL;DR and a 'what to do about it'.
- `/engineering-blog-finder` — Find reputable engineering blogs and specific high-quality posts on a topic — official vendor/product blogs and company engineering blogs (Microsoft, GitHub, AWS, Google, Netflix, Meta, Stripe, Uber, Cloudflare, and more) — with verified links, authors, and dates.
- `/feed-curator` — Recommend a personalized set of trustworthy sources and RSS/Atom feeds for the learner's stack — official blogs, release-note pages, engineering blogs, communities (Reddit, Stack Overflow, Discord), curated newsletters, LinkedIn creators/companies, and research venues — and export an OPML you can import into any reader.
- `/github-repo-finder` — Find high-quality, relevant GitHub repositories for a topic — official/reference implementations, well-maintained libraries, example projects, and curated 'awesome' lists — and assess each repo's health (stars, recent commits, releases, issues, license, maintenance).
- `/literature-review` — Survey multiple primary sources on a topic into a structured mini literature review: define scope, gather papers/docs, group by theme or approach, compare methods and findings, and separate consensus from open questions — all cited and dated.
- `/official-docs-finder` — Find the authoritative official documentation for a technology, product, API, or version — the canonical docs home, the exact relevant pages, the version/release-notes, and the API reference — with verified links and dates.
- `/paper-summarizer` — Summarize ONE research paper for a learner at their level: the problem, key idea, method, results, limitations, and why it matters — citing the exact paper (title, authors, venue/arXiv id, date) and never inventing findings.
- `/research-brief` — Produce a cited, dated research brief from official and primary sources — documentation, standards/RFCs, vendor engineering blogs, release notes/changelogs, official repos, and peer-reviewed papers.

### Security  `(31)`

- `/ai-agent-permissions-coach` — DEFENSIVE coach for containing excessive agency in LLM and agentic systems — tool allow-lists, scoped short-lived per-tool credentials, human-in-the-loop gates for irreversible actions, blast-radius limits, tamper-evident agent action audit trails, MCP/plugin trust boundaries, and unbounded-consumption quotas.
- `/api-security-coach` — Harden a web API defensively as a lesson — authentication and per-object authorization, input validation, rate limiting, output encoding, and the OWASP API Security Top 10 (2023).
- `/auth-designer` — Design authentication and authorization as a lesson — choose between sessions, JWT, OAuth 2.
- `/broken-access-control-coach` — DEFENSIVE coach for authorization design and verification — deny-by-default, object-level checks (IDOR/BOLA), function-level checks, multi-tenant isolation, and SSRF defense, plus choosing RBAC vs ABAC vs ReBAC and writing the tests that prove authorization holds.
- `/browser-security-model-coach` — Teach the browser's actual security model from the origin upward — Same-Origin Policy, what CORS does and does not grant, cookie SameSite and the __Host- prefix, CSRF defence-in-depth, framing/clickjacking control, Trusted Types, Subresource Integrity, and postMessage hardening — then emit a concrete header and cookie configuration the learner can ship and verify.
- `/cloud-iam-least-privilege-coach` — DEFENSIVE coach for cloud identity hardening across AWS, Azure, and GCP — find and close privilege-escalation attack paths, replace wildcard policies with scoped grants, apply permission boundaries and SCPs/deny policies, break role-chaining, swap long-lived keys for workload identity federation, and add JIT/PIM elevation with audit.
- `/compliance-control-mapping-coach` — Map one control obligation across frameworks instead of maintaining four spreadsheets — anchor on NIST CSF 2.
- `/cryptography-basics-coach` — Apply cryptography correctly as a lesson — hashing vs encryption, symmetric vs asymmetric, password hashing with argon2/bcrypt/scrypt, authenticated encryption, and why you must not roll your own.
- `/csp-headers-coach` — Configure HTTP security headers safely as a lesson — Content-Security-Policy, HSTS, X-Content-Type-Options, frame-ancestors/X-Frame-Options, Referrer-Policy — to reduce XSS and clickjacking, with a report-only rollout.
- `/dependency-audit` — Audit a project's dependencies for known vulnerabilities and supply-chain risk — SCA scanning, lockfiles, SBOM, pinning, and provenance — then remediate safely without breaking builds.
- `/detection-engineering-coach` — DEFENSIVE blue-team coach for detection-as-code — pick log sources, write and tune Sigma rules, map coverage to MITRE ATT&CK, cut false positives, version and CI-test detections, and measure real coverage instead of rule count.
- `/dfir-evidence-triage-drill` — Run a defensive digital-forensics and incident-response triage rep — collect in order of volatility (RFC 3227), keep an unbroken chain of custody, hash before you analyse, build a super-timeline, and scope the incident before anyone touches remediation — producing a triage record and a scoping statement, never attacker tooling.
- `/incident-postmortem` — Write a blameless incident postmortem that teaches — timeline, quantified impact, root cause via the 5 Whys, contributing factors, what went well/poorly, and tracked action items with owners.
- `/incident-response-drill` — Run a DEFENSIVE blue-team incident-response tabletop as a lesson — assign roles (IC, ops, comms, scribe), then walk detection, triage, containment, eradication, communication, and recovery for a simulated scenario, ending in a blameless postmortem.
- `/jwt-security-coach` — Use JSON Web Tokens securely as a lesson — pin the signing algorithm (never `alg:none`), validate signature and `iss`/`aud`/`exp`, handle expiry, rotation, and safe storage, and know when server sessions beat JWTs.
- `/oauth2-oidc-security-coach` — DEFENSIVE coach for safe OAuth 2.
- `/owasp-top10-explainer` — Explain the OWASP Top 10 (or a single category) for learning — with a minimal vulnerable-vs-fixed code example and the mitigation, so the learner recognizes and prevents the risk.
- `/phishing-resistant-auth-coach` — DEFENSIVE coach for phishing-resistant authentication — passkeys, WebAuthn/FIDO2 and smart cards, picking the right NIST SP 800-63-4 assurance level (AAL1-3), synced vs device-bound authenticators, and defending sessions against token theft, adversary-in-the-middle proxies, and MFA fatigue with token binding, continuous access evaluation, and revocation.
- `/post-quantum-crypto-migration-coach` — Plan a real post-quantum migration in the only order that works — inventory every use of public-key cryptography, score it against harvest-now-decrypt-later exposure, build crypto-agility, then swap in ML-KEM (FIPS 203), ML-DSA (FIPS 204) or SLH-DSA (FIPS 205), usually as a hybrid — with dates anchored to the published NIST transition guidance.
- `/privacy-by-design-coach` — DEFENSIVE coach for building privacy into a system — GDPR Article 25 and 35 in practice: data inventory and flow mapping, lawful basis, data minimization, purpose limitation, retention and deletion, DSAR handling, a worked DPIA, and LINDDUN privacy threat modeling.
- `/ransomware-resilience-drill` — Drill ransomware resilience from the recovery end backwards — 3-2-1-1-0 backup coverage, immutable/object-locked copies, an isolated backup control plane with separate credentials, and a timed restore test that actually proves RTO and RPO — because a backup you have never restored is a hypothesis.
- `/secrets-management-coach` — Manage application secrets safely as a lesson — keep keys and tokens out of code and git, choose env vars vs a secrets manager/vault, rotate and scope with least privilege, and detect leaks.
- `/secure-code-review` — Review code for security as a lesson — walk the OWASP Top 10 categories relevant to the snippet (injection, broken access control, secrets, crypto, unsafe deserialization) and, for each risk, show the vulnerable pattern, the fix, and the principle.
- `/secure-sdlc-maturity-coach` — Assess a software delivery lifecycle against NIST SSDF (SP 800-218) and OWASP SAMM v2, score it honestly, then pick exactly the next three controls and wire them into CI as gates with an owner and a break-glass path — maturity by evidence, not by aspiration.
- `/security-hardening-checklist` — Produce a prioritized, defensive hardening checklist for a system or service — configuration, authn/authz, secrets, network, logging, patching — each item mapped to CIS or NIST guidance so the learner knows the standard behind it.
- `/security-logging-audit-coach` — Design audit trails that survive both an investigation and a privacy review — decide what security events must be logged, redact secrets and personal data at the source, make the trail tamper-evident, set retention that matches your detection window, and alert on the events that mean something.
- `/sql-injection-defense` — Prevent SQL and NoSQL injection as a lesson — parameterized queries/prepared statements, safe ORM usage, input validation and allow-listing, and least-privilege DB accounts, with minimal vulnerable-vs-fixed examples.
- `/supply-chain-security-coach` — Secure the software supply chain — DEFENSIVE ONLY.
- `/threat-hunting-drill` — Run a timed, hypothesis-driven threat-hunting rep — pick one MITRE ATT&CK technique, write a falsifiable hypothesis about what it would leave behind in your telemetry, build the query, validate the hits against benign baselines, and convert the result into a durable detection or a documented telemetry gap.
- `/vulnerability-prioritization-coach` — DEFENSIVE coach for triaging vulnerabilities at scale — combine CVSS 4.
- `/zero-trust-architecture-coach` — Design a Zero Trust architecture that is implementable rather than aspirational — split the policy decision point from the enforcement point per NIST SP 800-207, score each CISA pillar against the Zero Trust Maturity Model stages, pick the one pillar to advance next, and define microsegmentation boundaries with an explicit failure mode.

### Statistics & experimentation  `(6)`

- `/bayesian-basics-coach` — Teach Bayesian thinking from first principles: turn a prior and a likelihood into a posterior with Bayes' theorem, summarize it with a credible interval, and contrast the Bayesian and frequentist views of probability.
- `/confidence-interval-coach` — Teach confidence intervals from first principles: what the confidence level really means (a property of the procedure, not the probability the parameter sits in this interval), what drives width, and how a CI relates to a p-value and to a Bayesian credible interval.
- `/experiment-analysis-coach` — Analyze an experiment's results honestly: quantify effect size and its confidence interval, weigh statistical vs practical significance, and catch pitfalls like peeking, sample-ratio mismatch, multiple comparisons, and Simpson's paradox.
- `/hypothesis-testing-coach` — Teach hypothesis testing from first principles: frame the null and alternative, choose the right test (one/two-sample or paired t-test, chi-square, nonparametric), compute and interpret p-values correctly, and reason about Type I/II errors and power.
- `/regression-diagnostics-coach` — Diagnose a fitted regression before trusting it: read residual plots for nonlinearity and heteroscedasticity, detect multicollinearity with VIF, and find influential points with leverage and Cook's distance.
- `/sampling-methods-coach` — Teach sampling from first principles: pick a probability design (simple random, stratified, cluster, systematic), size the sample from a target margin of error, and spot bias (selection, coverage, nonresponse, voluntary-response) that no sample size can fix.

### Teaching & curriculum  `(15)`

- `/cognitive-load-coach` — Redesign a lesson, slide, doc, or code walkthrough to fit working memory: separate intrinsic from extraneous from germane load, apply the worked-example effect, kill split-attention and redundancy, and fade scaffolding as expertise grows.
- `/confidence-calibration-coach` — Measure and fix the gap between how sure a learner feels and how often they are right: collect confidence-weighted answers, score them with the Brier score, plot a calibration curve, and diagnose over- or under-confidence with targeted drills.
- `/curriculum-designer` — Design a multi-module curriculum or course for a topic using backward design — define learning objectives with Bloom's verbs, sequence modules by dependency, and give each module outcomes, activities, and aligned assessment.
- `/exam-strategy-coach` — Coach how to take an exam under pressure — time budgeting per question, triage and flagging, eliminating wrong options, smart guessing, and managing test anxiety on the day.
- `/hackathon-planner` — Plan a hackathon project end to end for a fixed timebox — scope to what's actually buildable, assign roles, draw an MVP cut-line (must/should/won't), order the build as a vertical slice, and plan the demo.
- `/interleaving-planner` — Build mixed practice sets that train discrimination, not just execution: shuffle problem types so the learner must first decide which method applies, size the blocks, and know when blocking still wins.
- `/item-writing-coach` — Write assessment items that survive scrutiny: a clear stem, one defensible key, plausible misconception-based distractors, no cueing or clang or 'all of the above', and an explicit Bloom level per item.
- `/learning-objective-writer` — Turn vague goals like 'understand Kubernetes' into measurable objectives: pick an observable Bloom verb, write it in ABCD format (audience, behaviour, condition, degree), and constructively align objective to assessment to activity so nothing is taught that isn't assessed.
- `/lesson-plan-writer` — Write one concrete, teachable lesson plan — a single measurable objective, a hook, gradual-release (I-do / we-do / you-do), materials, minute-by-minute timing, checks for understanding, and homework.
- `/mistake-log-coach` — Turn errors into a compounding asset: capture every miss, categorize it (knowledge gap / misconception / process slip / misread), find the root cause, write the specific fix, and re-test it on a spaced cadence until it stops recurring.
- `/peer-review-coach` — Teach how to give and receive peer review of code, designs, or writing constructively — what to look for, how to phrase comments kindly and specifically, labeling nits vs.
- `/productive-failure-designer` — Design a struggle-before-instruction sequence in Kapur's productive-failure format: a complex problem attempted without teaching, learner-generated solutions collected, then a consolidation lecture that contrasts those attempts with the canonical method.
- `/reading-list-curator` — Curate a structured, sequenced reading and resource list for self-study — foundational → intermediate → advanced, each with why it matters, format, effort, and an honest quality note.
- `/retrieval-practice-coach` — Turn re-reading into remembering: build a retrieval set from source material, choose free recall vs cued recall vs short-answer, and lay it on an expanding spaced schedule with dated review sessions.
- `/self-explanation-prompter` — Generate prompts that force a learner to explain WHY each step works, connect it to prior knowledge, and notice their own comprehension gaps — the self-explanation effect (Chi et al.

### Testing & QA  `(14)`

- `/accessibility-audit` — Audit a UI against WCAG 2.
- `/api-testing-coach` — Test an API as a lesson — design happy-path, edge, and error cases; assert status codes, headers, and response schema; cover auth and idempotency; add contract tests; and automate it all in CI.
- `/bdd-scenario-writer` — Write behavior-driven scenarios in Gherkin (Given/When/Then) from real examples — capture behavior as concrete acceptance criteria, keep steps declarative not scripted, and map each scenario to a business rule.
- `/code-coverage-coach` — Interpret and improve code coverage meaningfully — line vs branch vs condition coverage, what coverage cannot tell you, and how to target the uncovered gaps that actually carry risk.
- `/contract-testing-coach` — Teach consumer-driven contract testing (e.
- `/e2e-testing-coach` — Design reliable end-to-end UI tests (Playwright/Cypress/Selenium) as a lesson — pick the few critical user journeys, use resilient selectors, wait on state not time, isolate data, and cut flakiness.
- `/flaky-test-fixer` — Diagnose and fix a flaky (intermittently failing) test — reproduce the flakiness, locate the nondeterminism (timing/async, test order, shared state, randomness, network), and make it reliable while explaining the root cause.
- `/load-testing-coach` — Design a load/performance test as a lesson — model realistic load, pick the right metrics (throughput, latency percentiles, error rate), choose ramp profiles (smoke/load/stress/soak/spike), and read the results to find the bottleneck.
- `/mutation-testing-coach` — Teach mutation testing to measure how good a test suite really is — what mutants are, how to read the mutation score, killing surviving mutants by adding assertions, and the technique's limits and cost.
- `/property-based-testing-coach` — Teach property-based testing (QuickCheck/Hypothesis) — how to find properties and invariants, write generators, use shrinking to get a minimal counterexample, and know when it beats example-based tests.
- `/tdd-coach` — Coach test-driven development on a task — write the failing test first (red), make it pass simply (green), then refactor, in small cycles, explaining how TDD drives design and keeps steps small.
- `/test-data-builder` — Design maintainable test data as a lesson — prefer builders/factories over inline literals, choose minimal vs realistic data, and keep tests isolated so order never matters.
- `/test-doubles-coach` — Teach the five test doubles precisely — dummy, stub, spy, mock, and fake — with crisp definitions, a decision table for choosing between them, the rule to mock only at boundaries you own (and never mock what you don't own), how to recognise over-mocking and brittle tests that assert implementation instead of behaviour, the London (mockist) vs Chicago (classicist) TDD schools, and when to verify behaviour versus state.
- `/test-plan-designer` — Design a test plan/strategy for a feature or release — define scope, risk-based prioritization, the test pyramid mix, environments, test data, and exit criteria, explaining each trade-off.

### Visualization & diagramming  `(10)`

- `/concept-map-generator` — Build a Novak-style concept map for a topic — a focus question, a ranked parking lot of concepts, a general-to-specific hierarchy, and labelled linking phrases so every edge reads as a true proposition, plus cross-links between branches that reveal real understanding.
- `/data-viz-coach` — Teach ONE chart from human perception upward — rank the visual encodings (position > length > angle > area > colour), enforce axis honesty, show uncertainty instead of hiding it, and prefer small multiples over an overloaded single panel — then emit a runnable, copy-pasteable Vega-Lite spec plus a one-sentence honest reading of the data.
- `/diagram-accessibility-coach` — Make any diagram usable without sight and without colour vision — short alt text plus a structured long description, a data-table equivalent, Mermaid accTitle/accDescr, redundant encoding beyond hue (labels, shape, pattern, position), contrast and text-size checks against WCAG 2.
- `/diagram-as-code-coach` — Choose and use the right diagram-as-code ecosystem — Mermaid vs PlantUML vs D2 vs Graphviz DOT vs Structurizr DSL — by matching the diagram's job to the tool's layout engine, then set up local rendering, CI checks, and a git-friendly workflow where diagrams live beside the code and review as text diffs.
- `/diagram-review-coach` — Critique a learner-drawn diagram like a design reviewer — check factual correctness first, then C4 level mixing, unlabelled or ambiguous edges, sync vs async confusion, node overload, missing trust boundaries, absent legends, and inconsistent shapes — and hand back a scored rubric plus a corrected redraw.
- `/dual-coding-coach` — Rebuild an explanation so words and visuals work as one channel instead of two — apply Mayer's multimedia principles (signaling, segmenting, coherence, pre-training, spatial/temporal contiguity, redundancy, modality) to cut split attention, kill decorative noise, and place labels where the eye already is.
- `/er-diagram-generator` — Generate a correct Mermaid erDiagram from DDL, ORM models, JSON samples or plain prose — entities and attributes with PK/FK/UK markers, precise crow's-foot cardinality, identifying vs non-identifying relationships, junction tables for many-to-many — plus normalization commentary (1NF→3NF, when to denormalize) and the questions the model can't answer yet.
- `/latex-math-coach` — Author math that renders correctly and reads clearly in KaTeX — inline vs display, aligned multi-step derivations, sums/products/limits, Big-O and complexity, matrices and cases, text inside math mode, spacing and delimiter sizing, plus the real KaTeX-vs-LaTeX gaps (no packages, no TikZ, no \\label/\\ref) and the accessibility story.
- `/sequence-diagram-generator` — Turn any protocol, API call chain, auth handshake or event flow into a correct Mermaid sequenceDiagram — lifelines and activations, autonumber, alt/opt/par/loop/critical/break fragments, async arrows, notes, and the failure path most diagrams forget.
- `/state-machine-visualizer` — Model any lifecycle as a correct Mermaid stateDiagram-v2 — composite (nested) states, fork/join for concurrency, choice pseudostates for guards, entry/exit notes and history — then back it with a full transition table and an explicit illegal-transition list you can turn into tests.

### Web3 & blockchain  `(6)`

- `/defi-primitives-coach` — Explain DeFi primitives conceptually — AMMs and the constant-product formula (x*y=k), liquidity pools and impermanent loss, lending/borrowing and collateralization, price oracles, and their risks (oracle manipulation, liquidation, slippage, MEV/front-running).
- `/gas-optimization-coach` — Teach EVM gas optimization without sacrificing safety — storage vs.
- `/nft-standards-coach` — Teach NFT standards on Ethereum — ERC-721 (unique tokens) and ERC-1155 (multi-token), the token interfaces, minting, tokenURI and metadata (on-chain vs.
- `/smart-contract-coach` — Teach smart-contract fundamentals on Ethereum/EVM from first principles — contract structure, state variables, functions and visibility, modifiers, events, custom errors, and testing locally with Hardhat or Foundry.
- `/solidity-security-coach` — DEFENSIVE Solidity security — teach the most common smart-contract vulnerabilities (reentrancy, integer overflow/underflow, broken access control, unchecked low-level calls, tx.
- `/web3-integration-coach` — Teach connecting a dApp frontend to Ethereum — wallets (MetaMask / EIP-1193), providers and signers with ethers.

### Writing & docs  `(6)`

- `/adr-writer` — Write an Architecture Decision Record for a specific decision — context, decision, status, consequences, and the alternatives considered — while teaching what makes an ADR useful.
- `/changelog-writer` — Turn a set of changes or commits into clear release notes or a Keep a Changelog-style changelog — grouped by Added, Changed, Deprecated, Removed, Fixed, and Security, with user-facing entries and clearly flagged breaking changes.
- `/documentation-planner` — Plan a documentation set with the Diátaxis framework — map audiences and their tasks to tutorials, how-to guides, reference, and explanation, then decide the writing order.
- `/readme-generator` — Produce a high-quality project README from the learner's real project details — title, one-line pitch, badges placeholder, features, install, usage/quickstart, configuration, contributing, and license.
- `/runbook-writer` — Write an operational runbook for a task or incident — prerequisites, step-by-step actions with expected output, verification, rollback, and escalation, with explicit safety notes on destructive steps.
- `/technical-writing-coach` — Coach the learner to improve a piece of technical writing as a lesson — pin down audience and purpose, fix structure, and cut jargon, passive voice, and ambiguity, naming the principle behind every edit.
